# EMIE 安全说明

EMIE 是内部业务系统，可能接触项目资料、人员信息、附件和生产数据库。安全问题应通过内部负责人私下报告，不要在公开 Issue、提交信息或文档中披露可利用细节和真实凭据。

## 支持范围

当前维护分支为 `project_manager_system`。安全修复应基于该分支验证，并按照正常发布流程部署。

## 需要立即报告的情况

- 服务器、数据库、飞书或其他平台凭据泄露；
- 未授权访问、权限绕过或公开分享越权；
- 文件上传、下载、预览或归档路径穿越；
- SQL 注入、脚本注入或敏感信息输出；
- 用户数据、项目附件或生产备份意外进入 Git；
- 生产数据库异常修改、删除或不可恢复。

## 处理原则

1. 不在公开渠道重复敏感值或攻击细节；
2. 立即隔离影响，必要时暂停相关发布；
3. 优先轮换已暴露的密码、token 或密钥；
4. 保留必要的时间、提交号和日志证据；
5. 完成修复、回归测试和影响评估；
6. 按发布手册部署，并记录验证和回滚条件。

删除仓库文件不等于凭据已经失效。若真实凭据曾进入 Git 历史，必须先轮换凭据，再评估是否清理历史。

## 敏感信息存放

- 本地服务器连接信息：`.server.local.env`
- 本地或 Compose 环境变量：`.env`
- 示例变量：`.env.example`，只能包含安全占位符
- 生产环境变量：由服务器本地文件或运行环境提供

上述真实配置不得提交。日志、截图、数据库导出和问题复现材料也必须先移除敏感数据。

## 发布要求

安全相关变更至少需要完成：

- 针对性回归测试；
- `./mvnw clean package`；
- 敏感信息和提交范围检查；
- 生产备份、部署后验证和日志检查；
- `docs/release-records.md` 留痕。
