# 更新日志

## v2.13 (2026-07-16)

### 🏷️ IP 二级选项

- IP 配置支持维护二级角色/子系列，并可为每个一级 IP 设置单选或多选规则
- 渠道定制单和公司常规品的新建项目页在未选择一级 IP 时隐藏二级区域；选择后以标签样式展示对应二级项
- 切换一级 IP 会自动清空原二级选择；后端校验二级项归属及单选限制
- 项目保存并展示完整的一级 IP 与二级 IP 信息，前端静态资源缓存版本更新至 `v137`

## v2.12 (2026-07-16)

### 🔔 工作流通知与飞书跳转

- 修复子任务派发后通知聚合 ID 为空，导致站内通知和飞书投递未创建的问题
- 通知投递独立事务执行；通知异常只记录日志，不影响项目和子任务业务保存
- 飞书卡片“查看并处理”改为完整公网 URL，并会在登录后自动打开对应项目详情
- 通知中心新增系统公网访问地址配置，便于域名变更后无需修改代码
- 修复新建子任务后项目详情未立即刷新，前端静态资源缓存版本更新至 `v136`

## v2.11 (2026-07-16)

### 🔔 通知中心基础能力

- 新增可靠通知 Outbox、站内通知、渠道投递和追加式通知审计基础模型
- 系统设置新增“通知中心”配置：站内/飞书渠道、重试、催办间隔、逾期升级和摘要策略
- 新增通知中心与飞书协同设计文档，为项目、子任务、审核、再次交付和催办全场景通知提供统一规范
- 通知中心配置新增“保存并发送测试”：向当前管理员创建站内测试通知；飞书已启用且账号绑定 Open ID 时，同时发送真实飞书卡片，并记录每个渠道的投递审计
- 修复飞书测试卡片 JSON 2.0 的 `body` 结构，确保真实发送可被飞书正确解析

## v2.10 (2026-07-16)

### ⚙️ Java 21 运行时与负载控制

- 启用可配置的 Spring Boot 虚拟线程，降低阻塞式 HTTP 请求的平台线程占用
- 收紧生产 Tomcat 并发上限和 Hikari 数据库连接池默认值，降低服务器峰值负担
- 保持飞书同步串行消费、文件预览单线程有界队列和外部服务限流
- 生产 JVM 堆上限调整为 192MB，应用 RSS 实测降低约 52MB
- 修复项目详情滚动回到图片区域时缩略图因跳过绘制而闪烁的问题

## v2.9 (2026-07-16)

### ☕ Java 21 基线升级

- Maven 编译基线升级至 Java 21
- Docker 构建镜像和运行时镜像统一升级至 Eclipse Temurin 21
- 开发环境、项目执行规范和发布文档同步更新为 Java 21
- 业务代码与数据库结构不变，生产环境需在 Java 21 容器验证通过后再切换

## v2.8 (2026-07-15)

### 🔎 五个业务列表页搜索与筛选

- 全部项目、渠道定制单和公司常规品支持关键词搜索、项目状态、产品类目、目标市场和要求日期筛选
- 我的子任务支持任务归属、任务状态、项目类型、关键词和计划完成日期筛选
- 待评分支持评分状态、项目类型、审核阶段、关键词和计划完成日期筛选
- 统一处理中文大小写、数组/JSON 数组字段和日期范围匹配，并提供一键重置
- 修复项目列表在全部/渠道定制/常规品之间切换时覆盖全量缓存，导致后续页面数据缺失的问题
- 前端静态资源缓存版本更新至 `v131`

## v2.7 (2026-07-15)

### 🗃️ 飞书主表镜像与永久备份

- 四张非 `_backup` 主表改为数据库严格镜像：每小时对账和管理员手动全量同步都会删除数据库中已不存在的系统同步记录
- 四张 `_backup` 表继续按业务 ID 增量更新且永不删除；源数据删除后保留完整记录，并标记“源数据已删除”和删除时间
- 主表、备份表统一写入“同步来源=系统”，清理时只处理系统记录，不影响飞书中人工维护的行
- 主表删除前必须确认对应系统备份存在并完成删除标记；飞书读取或备份检查失败时停止本轮清理，避免误删

### ✅ 两级审核同步

- 子任务交付时按项目类型建立两级审核记录：渠道定制为产品企划一审、销售二审，公司常规品为产品企划一审、管理员二审
- 子任务主表与备份表分别同步一审/二审角色、状态、审核人和得分，并保留“审核得分”作为两级评分的加权汇总
- 评分记录主表与备份表新增项目类型、审核阶段、审核状态、审核人、审核时间和审核意见，待审核与驳回记录不再丢失
- 项目主表与备份表新增审核流程、当前审核阶段和审核进度；审核记录变化会联动刷新项目与子任务汇总
- 新增飞书审核字段补齐接口和数据库迁移，兼容现有 8 张表及历史评分数据

### 🔄 飞书八表同步兼容

- 支持将项目、子任务、评分记录和操作日志同步到同一 Base 内的四张主表与四张备份表
- 子任务和评分主表的关联字段按飞书记录 ID 数组写入，备份表仍兼容文本或单选字段
- 日期字段统一使用毫秒时间戳，并跳过飞书自动创建时间等只读字段
- 操作日志由仅写备份表调整为主表与备份表双写

### 👥 自定义角色展示

- 用户管理列表、角色筛选和当前角色徽章统一读取角色管理中的中文显示名，自定义角色不再回退显示英文标识
- 为产品推广角色 `promotion` 增加紫色头像，以及带渐变背景、边框和阴影的彩色徽章样式
- 修复角色键包含大写字母时徽章样式失效的问题，`Promotion` 也能正确显示产品推广背景
- 组织架构的未分配用户列表支持显示自定义角色用户，新建和编辑部门的关联角色改为读取角色管理数据
- 组织架构中的自定义角色统一显示中文名称，并兼容角色键大小写的徽章样式
- 前端静态资源缓存版本更新至 `v130`

## v2.6 (2026-07-14)

### 🔐 飞书待授权账号

- 公司成员首次通过飞书登录时自动创建待授权账号，不再因系统未预建账号而直接登录失败
- 待授权账号只能查看授权状态页，后端统一禁止访问项目、文件、人员和管理等业务接口
- 管理员用户列表新增“待分配”状态和角色分配入口；分配正式角色后账号自动启用并清理旧会话
- 删除旧的自助注册接口、注册页面、前端注册逻辑及图片/邮件验证码端点，系统账号统一通过飞书认证建立
- 修复 OAuth 回调缺少会话时可能触发空指针的问题
- 前端静态资源缓存版本更新至 `v126`

### 🔄 飞书数据对账

- 项目、子任务和评分记录支持向可选的飞书备份表同步，主表与备份表按业务 ID 分别执行幂等更新
- 新增数据库到飞书的周期性全量对账，可补回飞书端被直接删除的记录
- 全量对账与 30 秒增量队列消费解耦，默认每小时且仅在队列空闲时执行，避免持续全量写入和新任务饥饿

### 🧩 前端模块化

- 将原 6791 行单体 `app.js` 按业务域拆分为 `core`、`dashboard`、`projects`、`admin`、`files` 和 `bootstrap` 六个脚本
- 将经典脚本升级为单一 `bootstrap.js` ES Module 启动链，业务函数不再挂载到 `window`
- 将认证、缓存、上传、管理和文件预览等跨模块可变状态收敛到 `window.EMIE` 分域状态容器
- 各模块通过 `EMIE.registerModule()` 显式声明公共接口，启动时校验模块是否完整加载
- 将 `projects.js` 继续拆分为上传、创建表单、项目详情、任务流程和项目分享五个子模块，原文件改为轻量项目域门面
- 继续将 `core.js`、`dashboard.js` 和 `admin.js` 拆分为职责子模块，所有业务文件控制在约 700 行以内
- 移除 297 个 HTML 内联事件属性，改为统一的 `data-emie-on*` 声明式事件入口
- 将散落的 `window._xxx` 缓存和业务状态收回 `EMIE` 分域状态容器
- 开发规范改为逐个检查 ES Module，静态资源缓存版本更新至 `v124`

### 🔐 权限与配置安全

- 修复销售附件权限查询未按销售账号过滤的问题，防止跨销售访问项目附件
- 统一项目列表、详情与附件的角色可见范围，严格区分设计师和供应链待认领任务
- 部门负责人可查看本部门同角色成员的项目、附件和忙碌状态，但不会自动获得项目修改权限
- 开发环境继续使用专用测试 MySQL，但连接信息只从本机 `.env` 加载；未指定 profile 时使用 H2 安全兜底，生产库必须显式提供环境变量
- 补齐项目详情、子任务等动态内容的前端输出转义，并加强管理员编辑用户时的字段校验
- 管理员图片上传不再允许 SVG，并限制单张图片最大 10MB
- 更新前端静态资源版本至 `v119`

### 🧪 自动化验证

- 新增销售附件隔离、角色隔离、部门负责人可见范围、仓库查询和数据库配置安全回归测试
- 新增前端模块布局、状态容器和模块公共接口合约测试
- 自动化测试由 11 项增加到 31 项

### 🐛 IP 配置排序

- 管理员新增 IP 配置时，排序号默认填写为现有最大排序号加一，避免所有新配置默认使用 `0`
- 更新前端静态资源版本至 `v118`，确保浏览器及时加载本次修复

### ⚙️ 发布流程

- 新增开发交接状态文档，要求每次进度、验证或运行状态变化后同步更新，支持其他 AI 或开发者随时接手
- 新增根目录 `RTK.md`，统一项目操作、验证、推送、生产部署与记录规范
- 新增代码推送与生产发布手册
- 新增生产发布记录模板，统一记录提交、数据库迁移、验证和回滚结果
- 新增项目 `README.md` 和文档索引，明确标准目录结构与文档入口
- 将项目导入模板生成脚本归入 `scripts/project-import/`，可交付模板归入 `docs/templates/project-import/`
- 完善 Git 与 Docker 忽略规则，隔离本地工具输出、运行数据、备份和敏感环境文件
- 新增 `.editorconfig` 和 `.gitattributes`，统一文本编码、缩进、换行符及二进制文件识别
- 完善仓库说明，新增开发指南、目录规范、协作说明和安全说明

## v2.5 (2026-07-02)

### ✨ 产品类目独立表

- **实体 + Repository**：新建 `ProductCategory` 实体（id/name/sortOrder/active）及 `ProductCategoryRepository`
- **Project 关联**：`Project.productCategory` 从 String 改为 `@ManyToOne` 关联 `ProductCategory` 实体
- **数据种子**：启动时自动插入 5 个默认类目（灯/音响/相机/个护/其他）
- **管理 API**：新增 `CategoryController`，提供类目 CRUD（`GET /api/categories`、`POST`、`PUT`、`DELETE`）
- **管理后台 UI**：新增「产品类目」tab，支持在线增删改类目，修改后自动刷新前端下拉选项
- **前端自适应**：新建项目时类目选项从 API 动态加载，不再硬编码

## v2.4 (2026-07-02)

### ✨ 新功能：渠道定制项目表单增强

- **产品企划必选**：新建渠道定制项目时，产品企划下拉框移除"未指定"选项，必须选择一名产品企划
- **新增产品类目选项卡**：在产品企划下方增加产品类目选择，选项包括灯、音响、相机、个护、其他；选择"其他"时弹出文字说明输入框且必填
- **新增目标市场选项卡**：在产品类目下方增加目标市场选择，支持国内、海外多选，至少选择一个
- **后端支持**：Project 实体新增 `productCategory`、`productCategoryNote`、`targetMarket` 字段；DTO、Controller、Service 全面对接数据存储与返回
- **列表展示**：渠道定制单列表和工作台概览增加产品类目、目标市场两列
- **详情展示**：项目详情页显示产品类目（含其他说明）和目标市场
- **草稿支持**：产品类目和目标市场选择支持草稿保存与恢复

## v2.3 (2026-06-26)

### 📱 移动端适配优化

- 工作台统计卡片改用 CSS 类（`sales-stats` / `four-col-stats`）替代内联样式，方便响应式重置
- 统计卡片最小宽度 200px → 180px，适配更小屏幕
- 移动端按钮统一缩小（`font-size:12px`），增加 `white-space:nowrap` 防止换行
- 筛选栏弹性布局：表单项 `flex: 1 1 auto`，支持换行自适应
- 表格移除固定 `min-width:500px`，改为内容自适应
- 页面标题 + 操作按钮区域移动端自动换行
- 480px 超小屏额外收紧内边距、字号、表格单元格间距
- 移动端布局高度新增 `-webkit-fill-available` 兼容 iOS Safari
- 新增 `.ft-bot` 样式用于卡片底部链接

### 🐛 iOS Safari 兼容

- 登录/注册表单 input 使用 `font-size:16px` 防止 iOS Safari 自动缩放
- 登录成功 + 页面刷新时重置 viewport meta（`maximum-scale=5.0`），解决输入框放大后不恢复的问题
- 切换页面后自动滚动到顶部（延迟确保渲染完成）

### 🔄 浏览状态保持

- 页面刷新后自动恢复上次浏览的页面（localStorage 持久化 `design_pm_lastView`）
- 管理后台恢复上次打开的 tab（`design_pm_lastAdminTab`）
- 关闭空闲倒计时 HTML 元素（功能正常但显示存在体验问题，后续优化）

## v2.2 (2026-06-26)

### 📱 移动端适配（基础框架）

- 新增汉堡菜单按钮，768px 以下自动显示
- 侧栏改为全屏覆盖面板 + 半透明遮罩，点击遮罩或导航项自动关闭
- Header 小屏精简布局，隐藏副标题
- 响应式断点 768px / 480px，覆盖表格/模态框/筛选栏/统计卡片/子任务卡片/详情页/表单等所有主要组件
- 管理员标签页支持水平滚动

## v2.1 (2026-06-26)

### ✨ 新功能

#### 系统操作日志
- 新增 `ActivityLogRepository` 数据访问层，支持日志持久化查询
- 登录/登出、项目查询等操作自动记录日志
- 管理后台新增「日志」查看 tab，支持按日期范围筛选
- 新增 `LogArchiveService` 归档服务：每月 1 日凌晨自动将上月日志压缩归档到 `logs/archive/`，查询时自动合并数据库与归档文件
- 管理员可通过 `POST /api/system/archive` 手动触发归档

#### 静止页面超时退出
- 60 分钟无操作自动退出登录，确保系统安全
- 最后 1 分钟在页面顶部显示倒计时提示
- 超时后弹出模态框提醒，需重新登录
- 支持键盘、鼠标、触摸、滚动等多种活动检测

### 🐛 Bug 修复

- 清理 `@Repository` 多余注解 — Spring Data JPA 自动实现，无需显式声明
- 移除 `DashboardController`、`ProjectController` 中未使用的依赖注入
- 移除 `ProjectService`、`UserService`、`ProjectSummaryDTO` 中未使用的 import
- 废弃导入清理，减少编译警告

### ⚙️ 工程优化

- 新增 `SchedulingConfig` 启用 Spring 定时任务支持
- 日志归档目录路径可配置（`app.log-archive.dir`）
- 添加 `logs/` 运行时目录到 `.gitignore`
