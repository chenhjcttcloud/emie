# EMIE 开发交接状态

## 2026-08-06 缩略图、连接池与飞书同步修复（待发布）

- 生产日志确认快速切换页面会并发请求 `/api/files/thumbnail/*`，权限校验加载完整项目/任务列表，导致 Hikari 主池 `active=20 idle=0 waiting=17`，产生 500/503。
- 本次修复详情页缩略图懒加载，并将文件权限校验优先改为可见项目 ID、项目/子任务文件引用计数和目标关联查询；保留历史兼容回退路径。
- `SyncWorker.processQueue()` 增加主库事务边界，修复飞书同步 `no Session` 懒加载失败。
- 版本 SSE 通道改为仅兼容旧客户端，新客户端使用已有 10 秒版本轮询，避免长期异步请求占用。
- `./mvnw test` 已通过（121 项）；已推送并部署，生产运行 SHA 为 `21886ba53775b7a05cf74225939090337f15765b`。健康接口 HTTP 200，发布后 3 分钟无新增严重错误；后续需重点观察缩略图 5xx、Hikari 等待数及飞书失败队列。

## 2026-08-06 无损缩略图与响应式显示（待发布）

- 缩略图缓存从 JPEG 改为 PNG 无损格式，最长边 640px；原图接口保持不变，缓存扩展名改为 `.png` 以避免复用旧 JPEG。
- 详情页缩略图尺寸改用 CSS `clamp` 和 `aspect-ratio`，适配不同屏幕宽度与分辨率；大图预览保持响应式限制。
- 本地 `./mvnw test` 已通过（121 项）；已推送并部署，生产运行 SHA 为 `916c90dcc7487332a914990bd8058025ba964811`，版本 `1.2.0`，健康接口 HTTP 200，发布后 3 分钟无严重错误。

## 2026-08-06 低分辨率桌面响应式补强（待发布）

- 针对 1280px、1100px、900px 宽度增加桌面端断点，收紧侧栏、主内容留白、统计卡片、项目表格和详情弹窗布局，兼容 4:3 与浏览器放大场景。
- 本地 `./mvnw test`、前端 JS 语法检查和 `git diff --check` 已通过；已推送并部署，生产运行 SHA 为 `55be0cbeb228a853a5092065a857dafa53c9de2a`，版本 `1.2.1`，健康接口 HTTP 200，容器 running/restart=0/OOM=false。

## 2026-07-31 本地开发与发布流程规范化（本地）

## 2026-08-03 子任务详情图片加载优化（本地）

## 2026-08-03 数据治理阶段启动（本地）

- 测试环境已升级 Spring Boot 3.4.12 + Flyway 11.8.1；本地服务已连接 `designpmtest` 并成功启动，迁移历史表正常读取，全量测试 121 项通过。本次尚未推送仓库或生产。

- 新增 `docs/data-governance-plan.md`，明确文件引用、交付版本和驳回记录的结构化方向、双写迁移、分批回填和回滚规则。
- 当前只做方案设计，不修改业务表、不回填历史数据、不执行生产迁移。

- 取消项目详情/子任务详情图片的懒加载，避免动态弹窗和滚动容器导致缩略图请求未触发。
- 缩略图加载失败时自动回退到授权原图；后端缩略图并发上限由 2 提高到 4，并记录生成失败文件和原因，便于继续定位特殊图片格式问题。
- 静态资源更新为 `project-uploads.js?v=158`、`project-detail.js?v=189`、`bootstrap.js?v=239`。本次仅修改本地，尚未推送或部署。
- 同步队列状态接口补充 `processing`、最近成功/失败时间和最近失败任务详情；相关单元测试已通过，尚未推送或部署。
- 管理后台概览已接入飞书同步状态卡片，静态资源更新为 `admin-shell.js?v=152`；本地检查通过，尚未推送或部署。
- 新增 `SyncQueueServiceTest`，覆盖同步队列健康统计；文件缓存和同步专项测试均已通过，尚未推送或部署。
- 新增驳回后重新交付的生命周期回归测试，验证审核重置和版本链不丢失；尚未推送或部署。
- 新增文件访问链路回归测试，验证缩略图缓存命中时不会重复解码源文件；相关专项测试通过，尚未推送或部署。
- 新增只读数据完整性扫描服务和管理员接口，覆盖项目、子任务、设计需求文件引用；新增 `DataIntegrityServiceTest`，尚未推送或部署。
- 管理后台概览已接入数据完整性扫描按钮，静态资源更新为 `admin-shell.js?v=153`；前端语法和结构测试通过，尚未推送或部署。

- 生产发布耗时优化已在本地完成：取消强制 `clean`、启用 SSH ControlMaster 复用、健康检查由 60 秒上限降为 45 秒；完整测试、数据库备份、产物校验和回滚边界保持不变。尚未推送或部署此轮优化。

- 本地验证与对外发布已拆成两套互不调用的流程：`scripts/dev-update.sh` 只负责静态缓存版本、Java 21 构建、测试服务重启和实际版本检查；不会提交、推送或连接生产。
- 发布确认后使用 `scripts/publish.sh --repository` 推送仓库，或使用 `--all` 在推送后调用既有生产原子发布脚本；发布脚本拒绝脏工作区和错误分支，不会自动提交未确认改动。
- 缓存版本由 `scripts/update-static-versions.sh` 按受影响的模块递增，并同步更新 `bootstrap.js` 入口版本；生产脚本继续负责候选容器、数据库备份、健康检查和回滚。
- 本次仅新增流程脚本和文档，尚未推送或部署生产。
- 首次生产预检因 `https://emie.emie.cn` 当前使用自签名证书而安全中止；发布脚本已增加显式 `SERVER_INSECURE_TLS=true` 开关，默认仍严格校验证书。
- 飞书内置浏览器继续缓存旧入口 `bootstrap.js?v=237`，导致未加载已更新的 `project-tasks.js?v=188`；入口版本已递增至 `bootstrap.js?v=238`，后续前端模块更新必须同步递增入口版本。

## 2026-07-31 驳回子任务日期选择修复（本地）

- 根因：驳回弹窗使用原始 `input[type=date]`，而全局样式隐藏了浏览器日期图标，且该输入没有统一的日期选择器触发逻辑，因此部分浏览器点击后不会弹出日历。
- 修复：日期输入接入 `showPicker` 触发逻辑，并增加统一样式的显式日历按钮；按钮通过 `triggerDatePicker` 兼容不支持 `showPicker` 的浏览器。
- 静态资源版本已更新为 `project-tasks.js?v=187`。本次仅修改本地代码，尚未提交、推送或部署生产。
- 日期控件进一步改为整块按钮式交互，选择后按钮显示具体日期；静态资源版本更新为 `project-tasks.js?v=188`。本次仍仅修改本地。

## 2026-07-30 生产快速发布脚本优化（本地完成）

- 根因复盘：此前仓库没有正式生产发布脚本，每次更新依赖临时命令；正常发布会重复上传、复制约 107MB JAR 并生成新镜像，且容器参数错误只能在停止旧容器后暴露。本次“蓝V联动海报”发布还暴露了环境变量尾部空项和回滚恢复后未立即退出两个脚本缺陷，导致一次无效切换和额外等待。
- 新增本地入口 `scripts/release-production.sh` 与服务器执行器 `scripts/release-production-remote.sh`。本地入口强制检查生产标识、SSH/sudo、内外网健康、业务分支、干净工作区、本地/远端完整 SHA，并执行 Java 21 全量构建；目标提交已运行时直接退出。
- 上传改为 rsync：服务器存在当前版本化 JAR 时先本地复制作为增量基线，失败可断点续传。服务器发布改为稳定 Java 21 运行时镜像 + `releases/<完整 SHA>/app.jar` 只读挂载，不再每次 `docker commit`；首次运行会兼容现有镜像式容器并额外备份旧 JAR。
- 候选容器会在旧容器在线时完成全部 18 项环境配置、网络、重启策略、上传/日志目录和 JAR 挂载检查，再进入约 22 秒的应用切换窗口。数据库备份、旧容器及版本化 JAR 回滚仍完整保留。
- 回滚使用明确状态机处理“旧容器已停止/已改名/候选已提升”三种阶段；恢复成功或失败都会立即退出，不再继续写入错误的运行提交。健康轮询从 2 秒缩短为 1 秒且隐藏预期的连接拒绝噪声。
- 新增 `ProductionReleaseScriptTest`，覆盖两个脚本 Bash 语法、候选先于停机、版本化只读 JAR、禁止逐版构建镜像、重复提交快速退出、目标 JAR 路径约束、空环境过滤和回滚退出保护。Java 21 `clean package` 共 117 项测试全部通过；对真实生产执行 `--preflight-only` 已验证当前 `55e222d`、sudo 及外网 HTTP 200，未构建、上传、重启或修改生产。
- 当前改动仅在本地工作区，尚未提交、推送或部署脚本。以后收到明确生产发布指令时，先配置被 Git 忽略的 `.server.production.local.env`，再使用新入口发布。

## 2026-07-30 “蓝V联动海报”文件权限修复（已发布）

- 用户强制刷新后确认设计/送审需求 `DR202607242846`（翻转相机）的设计师交付图“蓝V联动海报.png”仍未显示，因此排除单纯浏览器缓存问题。
- 生产数据库与上传目录此前已确认：详情记录包含 1 张交付参考图，原文件 `59a0e4ed-2d3c-4154-b489-6d3ac5c42086.png` 存在、格式为有效 PNG、尺寸 `1242×1660`。本轮进一步定位到统一文件鉴权只检查项目和子任务 JSON，没有检查独立 `design_requirements` 表；设计师本人因文件 owner 身份通常可读取，产品企划、产品推广等需求参与者则会被缩略图/原图接口误判为 403。
- `DesignRequirementRepository` 新增按当前用户参与关系和文件名查询引用的方法，覆盖需求参考图、需求附件、交付参考图和交付附件；`FileController` 将该规则纳入历史未绑定文件、已绑定文件和缺少文件记录三条统一访问路径。管理员仍按既有规则访问，非参与人员仍不可读取。
- 新增回归用例，验证产品企划可以读取由设计师上传、且 owner 不同的设计需求交付图片。专项测试通过；Java 21 `clean package` 共 111 项测试通过，无数据库结构或数据迁移。
- 功能提交 `55e222d5b6b7071b10f4f0ada382093967ed0705` 已推送 Gitee 并部署到 `192.168.200.51`。生产备份位于 `/home/emie/emie-deploy-backups/20260730_183640/`，数据库备份 217458 字节且 `gzip -t` 通过；旧 JAR SHA-256 为 `cc7ed409dc3d3251435c2a0c478f0f6e73935116eda2f494a6bca13176f68469`，旧镜像和停止容器均保留可回滚。
- 首次容器切换因运行环境列表末尾空项被误作 `--env` 参数而被 Docker 拒绝；旧版应用已自动恢复 HTTP 200，数据库和上传目录没有改动。修正空项过滤及“回滚后立即退出”保护后重新发布成功；最终生产镜像为 `emie-app:55e222d`，容器 running、重启次数 0，运行 JAR SHA-256 为 `eb5923e89da5c6391fe3e38d5653513edbfd8b8b968dd9a96586bd6a097dcc1c`，本机公开配置和外网 HTTPS 均 HTTP 200，启动后应用错误 0。
- 生产再次核验目标需求仍包含 1 张交付参考图，JSON 引用目标文件且原文件为 1430008 字节；未登录缩略图和原图接口按预期返回 401。当前没有可用的需求参与者登录凭据或可接管浏览器会话，因此最终登录态图片显示需用户重新打开详情确认。

## 2026-07-30 设计/送审需求资料显示修复（已发布）

- 诊断阶段只读核对生产数据库、上传目录和当前前端实现，没有修改生产数据或线上应用。
- 用户最终确认本次反馈目标为设计/送审需求 `DR202607242846`（翻转相机），不是最初诊断时发现的 `DR202607307885`（小黄人拍拍灯）。生产数据库中“翻转相机”关联 3 张需求参考图和 1 张设计师交付参考图“蓝V联动海报”；4 个 PNG 原文件均存在且格式有效，尺寸分别为 `680×901`、`620×563`、`682×909`、`1242×1660`，不存在上传丢失或文件损坏。
- 根因是“设计/送审需求详情”前端展示遗漏：后端详情接口已经返回需求参考图、需求附件、交付参考图和交付附件，但 `openDesignRequirementDetail` 只渲染基本文字、交付文字和评分，没有渲染上述四类文件，因此用户看到的是“未显示”而非“未上传”。
- 该展示遗漏实际影响所有创建设计/送审需求的销售、产品企划和产品推广，并非只针对产品推广；产品推广因更常上传海报类参考图而率先暴露。产品推广历史问题较多的结构性原因是角色兼容值仍包含 `Promotion`、`promotion`、`product_promotion` 等别名，且角色规范化、标签、权限和业务分支分散在多个前后端模块中，新增页面容易遗漏其中一条路径。
- 已在设计/送审需求详情增加独立“需求资料”区域，并在“交付与评分”中补齐设计师交付参考图和附件；复用项目/子任务统一文件组件，因此图片继续支持放大、原图复制和下载，附件继续支持预览与下载。该展示不按创建人角色分支，销售、产品企划、产品推广、指定设计师和管理员读取相同详情数据。
- 静态资源更新至 `dashboard-lists.js?v=187`、`bootstrap.js?v=237`；增加前端结构回归断言，全部前端 JavaScript 语法检查、`git diff --check` 与 Java 21 `clean package` 已通过，共 110 项测试。
- 功能提交 `78416c3bcb6a039bce361288b127c8ae2c6a5b6a` 已推送 Gitee 并部署到 `192.168.200.51`；生产容器、JAR 校验、内外网入口、静态资源版本和需求 `DR202607242846` 的 3 张需求参考图 + 1 张交付参考图关联均验证通过。首次切换因运行配置提取规则漏掉数据库变量而失败，已立即回滚并恢复 HTTP 200；逐项重建并校验 12 个业务变量后再次发布成功。最终容器重启次数 0、日志无严重错误，数据库未受影响；回滚点位于 `/home/emie/emie-deploy-backups/20260730_175700/`。

## 2026-07-30 生产服务器重启自启动检查

- 只读核对 `192.168.200.51`：Docker 与 containerd 均为 systemd `enabled`、当前 `active`。
- 正式应用 `emie-app:1c2143d` 当前 running、重启策略为 `unless-stopped`；MySQL、Redis、OpenResty、Certimate 均 running 且为 `always`，服务器正常重启后会由 Docker 自动拉起。
- 两个历史回滚容器均为手动停止的 `exited + unless-stopped`，服务器重启后不会自动启动或与正式应用争抢 8080；当前仅正式 Java 进程监听 8080，公开配置健康检查 HTTP 200，生产 `release-sha.txt` 为 `1c2143db1c5f616aad14e6577cbbf65c06c92245`。
- 重启后仍应等待约 1–3 分钟，再核对五个正式容器、应用 8080、外网 HTTPS 和重启次数；本轮没有执行服务器重启或修改容器配置。

## 2026-07-30 生产飞书多维表格全量/增量同步诊断

- 本轮只读检查生产日志、`sync_queue`、同步游标及飞书八张主/备份表，没有触发全量重刷、人工入队或修改生产数据。
- 增量队列能够消费且最终清空，当前无 `pending`、`processing`、`fail`；但 16:08 的 20 条批次耗时约 6 分钟，后续 10 条又耗时约 5 分钟。根因是每同步一条记录都会分页扫描整张主表和备份表查找业务 ID，操作日志接近 3000 条后形成近似 O(N²) 的请求量；HTTP 调用串行且 20 条共用一个长事务，处理中数据库仍显示整批 `pending`。
- 定时“全量对账”并非真正补齐全量：它会扫描全表删除飞书主表孤儿，但补记录阶段只重新入队 `feishu.sync.cursor` 之后更新的数据。生产 14:07 曾发生一次镜像对账请求超时；16:08 成功推进游标并入队 30 条，但无法恢复较早被飞书主表删除且数据库未再次更新的记录。
- 队列清空后的只读集合核对：数据库/主表分别为项目 `53/53`、子任务 `84/82`、评分 `104/97`、操作日志 `2883/2843`。主表缺少子任务 ID `78,79`、评分 ID `63,64,65,66,68,69,70`、日志 ID `2210–2249`，无额外 ID 或重复 ID。缺失的 7 条评分和 40 条日志完整存在于备份表；2 条子任务不在主表或备份表，但数据库原始记录仍存在。
- 活动日志没有实体同步监听器，只能依赖每小时游标扫描，天然存在最长约一小时延迟。下一步建议修复全量补齐语义、把记录定位改为飞书服务端精确搜索或本地业务 ID 映射、缩短队列事务并记录每批耗时/进度，之后再执行一次受控全量修复缺失主表记录。

## 2026-07-30 交付成果弹窗缓存兼容热修（已发布）

- 根因已确认：`core-runtime.js?v=155` 在 7 月 27 日已被长期缓存，7 月 29 日加入 `EMIE.fileAccept` 时未递增该资源版本；生产静态资源设置了 30 天 `immutable`，导致部分设计师浏览器同时运行旧核心模块和新交付模块，打开交付弹窗时读取 `EMIE.fileAccept.reference` 报错。
- `dashboard-lists.js`、`project-form.js`、`project-tasks.js` 的上传类型读取均增加安全默认值，即使旧核心缓存混入也不会让弹窗崩溃；核心运行时、三个消费模块和启动入口同步递增缓存版本。
- 增加前端结构回归断言，确保上传弹窗不再直接读取可能缺失的 `EMIE.fileAccept.reference`；全部前端 JavaScript 语法检查及 Java 21 `clean package` 已通过，共 110 项测试。
- 功能提交 `1c2143db1c5f616aad14e6577cbbf65c06c92245` 已推送 Gitee 并部署到 `192.168.200.51`；生产容器、内外网入口、JAR 校验值、资源版本、旧缓存兜底及依赖容器均验证通过。数据库与旧应用回滚点位于 `/home/emie/emie-deploy-backups/20260730_115730/`。

## 2026-07-30 原图文件跨应用拖拽（已发布）

- 在快捷键复制已改为保留原图 Blob 的基础上，跨应用拖拽复用同一原图缓存；图片放大或鼠标进入缩略图时提前异步读取原图，避免在 `dragstart` 后才读取而错过浏览器唯一可写拖拽数据的同步窗口。
- `dragstart` 会构造带原始 Blob、MIME 和文件名的 `File` 并加入 `DataTransfer.items`，使 PowerPoint/WPS 优先按真实图片文件导入并保留像素、DPI 和宽高比；同时提供 Chrome `DownloadURL`、`text/uri-list`、`text/plain` 和 `text/html` 兼容降级。
- 删除原先最长边 1200px 的 Canvas 拖拽载荷；Canvas 只决定拖动浮层的旧方案不能控制外部应用实际收到的文件，也会造成尺寸语义不一致。
- 原图尚未准备完成时会取消本次拖动并提示稍后重试，避免悄悄把缩略图或错误格式送入 PowerPoint。浏览器向外部应用输出文件仍受浏览器和操作系统支持差异影响，需以用户实际 Firefox/Chrome → PowerPoint/WPS 链路验收。
- 前端缓存更新至 `project-uploads.js?v=157`、`bootstrap.js?v=235`；前端语法与 Java 21 `clean package` 共 110 项测试全部通过。功能提交 `d14e494efc17d7b44dbfaa1a83f484fe80ebadec` 已推送 Gitee 并部署到 `192.168.200.51`，内外网入口、资源版本、JAR 校验值和依赖容器均验证通过；生产真实 PowerPoint/WPS 拖拽仍需用户最终验收。

## 2026-07-30 生产 HTTPS 快捷键复制比例修复（已发布）

- 用户实测生产 HTTPS 中第一次右键复制正常，但 `Command/Ctrl + C` 粘贴尺寸异常，随后再次右键复制也可能异常。抽查同步原图为 4000×2400、300 DPI；原快捷键实现先缩至最长边 2400px 并经 Canvas 重新编码，导致 DPI 元数据丢失，PowerPoint/WPS 会按默认 72/96 DPI 解释位图尺寸。
- 快捷键复制现优先 `fetch` 当前原图并把服务器返回的原始 PNG Blob 直接写入剪贴板，不缩放、不经 Canvas，因此保留像素、DPI 和原始 PNG 元数据；浏览器支持原格式时其他图片格式也直接写入，不支持时才等尺寸转为 PNG。
- 删除失败时选中页面图片并执行 `execCommand('copy')` 的降级路径；失败只提示使用浏览器右键复制，不再留下 DOM 选区或污染下一次原生复制。
- 快捷键复制修复最初验证版本为 `project-uploads.js?v=156`、`bootstrap.js?v=234`，后续与原图文件拖拽一起更新至更高版本；前端语法及 Java 21 `clean package` 共 110 项测试全部通过。已随功能提交 `d14e494efc17d7b44dbfaa1a83f484fe80ebadec` 推送并部署生产，用户已确认生产 HTTPS 下快捷键和右键连续复制正常。

## 2026-07-30 测试环境图片复制与跨应用拖拽诊断

- 图片文件同步和归档状态兼容修复没有删除复制或拖拽代码；当前 `project-uploads.js?v=155` 仍包含放大图快捷键复制与 `dragstart` 处理。
- 测试入口为 `http://192.168.200.118:8080`，属于非安全上下文；浏览器不会开放 `navigator.clipboard.write` 的 PNG 位图写入能力，因此 `Command/Ctrl + C` 只能退化为选中图片并提示右键复制，无法获得生产 HTTPS 环境的高清位图剪贴板行为。
- 跨应用拖拽当时仅向 `DataTransfer` 写入 HTML，PowerPoint/WPS 对浏览器 HTML 拖拽和带认证图片 URL 的支持不稳定；后续已在本地实现提前加载原图并同步提供真实文件载荷，见上方“原图文件跨应用拖拽”记录。
- 本轮为原因诊断，未修改复制或拖拽实现。

## 2026-07-30 测试环境项目图片同步与归档状态兼容

- 测试数据库此前由生产数据库覆盖，但生产上传目录没有同步到本机测试目录：测试目录原有 31 个文件、约 68MB，生产 `/home/emie/emie-app-data/uploads` 根目录有 268 个业务文件、约 1.4GB。
- 已按“生产 → 测试、只补缺失、不覆盖、不删除”的方式同步 267 个缺失文件，共 1,408,209,893 字节；逐个核验生产清单后结果为缺失 0、大小不一致 0。测试目录保留原有测试文件，根目录现有 289 个文件。
- 同步后继续发现：生产数据中的部分文件记录为 `archived` 且没有可用归档路径，原图下载因控制器优先读本地文件可用，但缩略图和文件信息接口经 `FileArchiveService` 时仍无条件尝试 NAS 恢复，分别返回 422/500。
- `FileArchiveService.resolveFile` 已改为不论数据库存储层级，只要文件在上传目录或恢复缓存真实存在就优先返回；新增“已归档记录 + 本地同步图片”的解析和缩略图回归测试。
- Java 21 `clean package` 共 110 项测试全部通过，其中新增 2 项归档文件本地优先与缩略图回归测试；本地测试服务已重启并在 `http://localhost:8080` 就绪。本次代码与文档仅在本地，未提交、未推送、未部署生产。

## 2026-07-29 项目参与者查看完整子任务链

- 项目详情取消按当前执行角色过滤子任务：只要用户作为销售、产品企划或任一子任务负责人具备该项目详情权限，即可看到项目内设计师、供应链、产品推广等角色的全部子任务及历史交付内容。
- 本次仅扩大项目内协作信息的读取范围；接单、交付、重新交付、编辑、删除和审核按钮继续按任务负责人、项目负责人及审核角色判断，不扩大写权限。
- 增加前端结构回归断言，防止以后重新按设计师/供应链角色隐藏项目内其他任务。静态资源更新至 `project-detail.js?v=188`、`bootstrap.js?v=233`；已随提交 `2835d08ab4749cc8f07a2ffb5d339a961dc23cdc` 推送 Gitee 并部署生产。

## 2026-07-29 WPS 快捷键复制原图

- 根因：浏览器右键“复制图像”写入的是原生图片位图，原快捷键方案写入的是 HTML；WPS 对两种剪贴板格式采用不同尺寸解析，导致快捷键复制后比例不稳定。
- 放大图激活时，`Command/Ctrl + C` 在 HTTPS 安全上下文中改为通过原生剪贴板 API 写入标准 PNG 位图，不再写 HTML；每次复制都根据当前原图重新生成，避免连续复制继承前一次尺寸。
- 已用实际 `1800 × 2400` 图片在 Firefox → WPS 链路连续验证两次，WPS 两次均插入为 `14.29 × 19.05 cm` 并锁定纵横比。局域网 HTTP 测试地址受浏览器安全策略限制，页面会明确提示使用右键复制；正式 HTTPS 域名可使用快捷键。
- 拖拽跨应用功能本轮按用户要求暂缓。静态资源更新至 `project-uploads.js?v=155`，已随提交 `2835d08ab4749cc8f07a2ffb5d339a961dc23cdc` 推送并部署生产。
- 生产 `emie-app:2835d08` 运行稳定、重启次数 0，内外网入口与静态资源验证通过；数据库和旧应用回滚备份位于 `/home/emie/emie-deploy-backups/20260729_185154/`。

## 2026-07-29 执行角色工作台与子任务驳回期限

- 设计师、供应链和产品推广工作台不再展示项目列表，改为按渠道定制单、公司常规品分类展示当前用户负责的待接单、设计中和已驳回子任务；每类优先显示 5 条并可进入“我的子任务”查看全部。
- 工作台复用统一子任务状态字典，并同时过滤已完成子任务及所属项目已完成、待终止、已终止的记录。
- 驳回子任务时必须选择新的要求完成时间，后端同步校验并更新子任务计划完成日期，驳回快照记录该期限。
- 图片预览与缩略图拖拽曾尝试统一保持宽高比的数据；实际 Firefox → WPS 跨应用拖拽仍不稳定，已在后续快捷键方案中明确暂缓。
- 静态资源入口更新为 `bootstrap.js?v=228`；Java 21 `clean package` 共 108 项测试、全量前端 JavaScript 语法和差异检查通过。
- 功能提交 `65d6de9dd3751838d50ddbe04f370189d6e79190` 已推送 Gitee 并部署到 `192.168.200.51`；生产容器、内外网入口、静态资源版本和启动日志均验证通过，数据库与旧应用回滚备份位于 `/home/emie/emie-deploy-backups/20260729_174843/`。

## 2026-07-29 设计师工作台与测试数据准备

- 设计师工作台项目总预览已增加待接单、进行中/待修改、已完成三类数量总览卡片；任务卡片继续按逾期、待修改、待验收和完成状态排序。
- 上传体验已统一：参考图最多 6 张、单文件 200MB；附件支持 AI/STEP/STP、办公文档、压缩包和图片；所有上传区域显示格式与大小提示。
- 交付图片预览支持放大和拖拽到 PowerPoint，正在继续优化跨浏览器拖拽尺寸一致性。
- 下一步：在覆盖测试库前先备份 `designpmtest`，再以生产 `designpm` 数据覆盖测试库，供工作台回归测试使用。

## 2026-07-28 产品推广“我的子任务”修复发布

- 提交 `4b150f50c053ff694bf8883e139034c493eb080a` 已推送并部署到 `192.168.200.51`。产品推广被派发子任务后会进入“我的子任务”卡片视图，生产外网 HTTPS 与容器健康检查通过。

## 2026-07-28 子任务通知与配置管理修复发布

- 提交 `ec44b025cb1994c02f5246d6c20e875cf4f343dc` 已推送并部署到 `192.168.200.51`。子任务创建先独立持久化并取得真实 ID，通知失败不会再导致主业务返回 400；产品推广已具备子任务接单、交付和重新交付权限。
- 系统配置页明确展示并可编辑“飞书通知跳转地址”，后端仅接受完整 HTTP(S) 地址；数据库类型与连接地址以中文展示。生产当前跳转入口为 `https://emie.emie.cn:9001`。
- 生产应用 `emie-app:ec44b02` 运行正常、重启次数 0，公开配置与外网 HTTPS 入口 HTTP 200，JAR SHA-256 与本地构建一致。回滚镜像为 `emie-app:backup-20260728_105426`。

## 2026-07-27 本地生产发布

- 功能提交 `20c4399ae6e9c16333f5efd157bdc88defe01d00` 已推送 Gitee `project_manager_system`，并部署到本地生产服务器 `192.168.200.51`。
- 本地生产数据库 `designpm` 已备份并执行权限基础、子任务交付版本迁移；最终为 71 项权限和 34 条交付版本。
- 生产容器使用 `emie-app:20c4399`，重启次数 0；JAR SHA-256 为 `d31715821aef84cb3ec3cdb88fa2a16eba07fce200fe63696260ed7e6044911d`。本机与外网 HTTPS 健康检查均通过，首页加载 `bootstrap.js?v=227`。
- 云端服务器仍保持原提交 `66346838f8183c5f5a276d3b2fbd6955e396c375`；一次目标识别偏差造成的停服和增量迁移已通过备份恢复、清理新增表并重新验证 HTTP 200，云端未替换应用产物。

## 2026-07-27 子任务版本化交付

- 新增不可变的 `sub_task_delivery_versions` 交付版本表；首次交付、负责人主动修正和驳回后重交分别生成 `initial`、`correction`、`redelivery` 版本，现有 `sub_tasks` 交付字段继续作为当前版本以兼容旧页面和飞书同步。
- 审核完成前，子任务负责人可使用“修正交付”保留正确文件、移除误传文件、补充漏交文件，并必须填写修正说明；新版本提交后任务回到 `delivered`，全部旧审核结果失效并从企划一审重新开始。已完成任务禁止直接修正。
- 项目详情和“我的子任务”详情均展示完整交付版本历史，包含版本类型、修正说明、提交人、自评、文字成果、参考图和附件。
- 迁移 [`2026-07-27-add-sub-task-delivery-versions.sql`](../db/migrations/2026-07-27-add-sub-task-delivery-versions.sql) 可重复执行，并会把历史已有交付初始化为 V1；已在 `192.168.200.51/designpmtest` 备份后执行，初始化 3 条历史版本。
- 106 项测试和相关前端模块语法检查通过；已随 `20c4399ae6e9c16333f5efd157bdc88defe01d00` 推送并部署本地生产。

## 2026-07-24 子任务驳回资料与工作流通知修复

- 子任务驳回弹窗新增参考图、附件上传；后端分别以 `rejectionReferenceImagesJson`、`rejectionAttachmentsJson` 保存到该次 `ActivityLog.afterData`，文件继续绑定子任务，驳回记录详情会展示该次驳回方补充的资料。
- 新增 `DESIGN_REQUIREMENT_ASSIGNED` 通知模板；设计/送审需求保存并初始化评分后，向指定产品企划创建站内通知并按系统配置投递飞书。
- 子任务发布通知改为使用刚创建的任务实体及其真实 ID，不再按“名称 + 负责人”反查可能命中的历史同名任务。
- 设计需求和子任务指派均通过 `notifyUserAfterCommit` 在业务事务成功提交后发出，避免业务回滚后产生无效通知，或通知先于业务数据可见。
- 已补充设计需求通知收件人、子任务精确负责人及驳回资料快照自动化测试；Java 21 `clean package` 共 104 项测试、全部前端 JavaScript 语法及 `git diff --check` 通过。
- 本地服务已重启为 PID `30470`，首页 HTTP 200，加载 `bootstrap.js?v=223`，启动日志无应用错误。未提交、未推送、未部署。

## 2026-07-24 权限系统：高级治理与发布准备

- 角色管理现可为 `project.view`、`project.detail.view`、`subtask.view` 配置 `own`、`participated`、`department`、`role_team`、`all` 数据范围；保存时权限与范围进入同一版本快照和审计记录。
- 新增权限治理接口和页面：用户能力预览、用户 + 权限 + 项目的模拟判定、原因链、最近 100 条权限变更以及空角色、自定义角色权限过大、读取权限缺少范围等异常检测。
- 历史版本可按审计 ID 回滚；服务端校验历史目标角色和快照完整性，回滚要求填写原因，并生成更高版本的 `role.rollback` 审计，不覆盖或删除原记录。
- 新增 [`permission-release-checklist.md`](permission-release-checklist.md)，固化生产备份、幂等迁移、六角色验收、发布观察和应用/权限配置回滚流程。
- Java 21 `clean package` 共 102 项测试通过，全部前端 JavaScript 语法和 `git diff --check` 通过。权限迁移在共享测试库再次连续执行两次，结果稳定为 71 项权限定义、162 条允许授权、18 条范围、6 个角色版本。
- 本地服务已重启为 PID `29371`，首页 HTTP 200，加载 `bootstrap.js?v=222`，启动日志无应用错误；未认证访问权限治理接口返回预期的 401。未提交、未推送、未部署。

## 2026-07-24 权限系统：其余写操作与系统管理授权

- `ProjectController` 的项目接单、流程推进/送审、流程审核、暂停、恢复、取消终止、终止、删除和分享，以及子任务编辑、接单、交付、重新交付、两级通过/驳回、评分和删除均已在业务服务执行前校验稳定权限编码。
- 子任务审核权限按当前审核角色解析为企划首审、渠道销售二审、常规品管理员二审；权限校验之后仍由 `ProjectService` 校验具体项目归属、指定审核人、任务状态和审核轮次。
- 设计/送审需求的交付、自评和复评分别接入 `design_requirement.deliver`、`design_requirement.score.self`、`design_requirement.score.review`；后端仍校验指定设计师和本轮评分记录。
- 管理员身份切换改为校验 `admin.identity.switch`。系统管理过滤器按接口映射到配置、资源上传、用户、用户角色、角色、通知、评分权重、数据清除、工作量、文件归档、飞书同步、目录、组织、监控、项目导入和分享管理权限，不再仅判断角色字符串。
- 对已有 `AuthController.isAdmin` 业务守卫，过滤器仅在当前请求通过对应管理权限后写入可信授权标记，因此自定义管理角色可以使用被授予的单项功能，未授权请求在到达 Controller 前返回带权限编码的 403。
- 为保持迁移前行为，幂等迁移补齐项目暂停/恢复/终止/分享、设计需求复评、项目审核和评分等默认授权。共享测试库现有允许授权共 162 条：管理员 50、设计师 19、产品企划 32、产品推广 17、销售 27、供应链 17；数据范围仍为 18 条。未更新生产。
- Java 21 `clean package` 共 100 项测试通过，全部前端 JavaScript 语法检查及 `git diff --check` 通过；本地服务已重启为 PID `27946`，首页 HTTP 200，启动日志无应用错误。

## 2026-07-24 权限系统：项目与子任务数据范围

- `PermissionService` 现在解析并返回权限对应的数据范围，规范化 `role_permission_scopes` 有配置时优先于兼容模板；首批支持 `all`、`own`、`participated`、`department`、`role_team`。
- `ProjectAccessService` 已用数据范围统一约束项目全量列表、服务端分页、数量统计、可见 ID、带子任务项目查询和项目详情。禁用 `project.view` / `project.detail.view` 时不会因默认范围继续返回数据。
- 项目分页的 Criteria 查询继续在数据库层拼接范围条件；执行类自定义角色会按子任务负责人角色和用户关系过滤，不再默认返回空列表。产品推广的参与项目范围也已纳入统一执行角色逻辑。
- 部门子任务查询读取 `subtask.view` 的 `all`、`department` 或 `role_team` 范围；“我的子任务”仍严格限制为本人负责或本人发布。
- 能力接口的 `scopes` 已被前端状态接收，提供 `hasDataScope(permission, scope)` 供后续界面解释和范围配置使用。
- 幂等迁移为六类现有角色的三个读取权限写入 18 条默认范围：管理员 `all`，产品企划 `role_team`，销售/设计师/供应链 `department`，产品推广 `participated`。共享本地测试库已执行并逐角色核验，未更新生产。
- Java 21 `clean package` 共 97 项测试通过，全部前端 JavaScript 语法检查及 `git diff --check` 通过；本地服务已重启为 PID `26763`，首页 HTTP 200，并加载 `bootstrap.js?v=220`。

## 2026-07-24 权限系统：项目编辑与子任务创建

- `PUT /api/projects/{id}` 已按项目类型校验 `project.channel.edit` / `project.regular.edit`，之后继续执行所属销售或所属产品企划的归属校验。
- `POST /api/projects/{id}/tasks` 已校验 `subtask.create`，之后继续执行原有产品企划、管理员、项目负责人和项目状态规则。
- 项目详情的“编辑项目信息”“添加子任务”入口以及弹窗二次入口均接入相同权限编码，后端仍是最终安全边界。
- 修正权限基线遗漏：销售默认获得 `project.channel.edit`；共享本地测试库已重新执行幂等迁移并验证该授权为 `allow`。未更新生产。
- Java 21 `clean package` 共 94 项测试通过，全部前端 JavaScript 语法检查及 `git diff --check` 通过；本地服务已重启为 Java PID `25469`，首页 HTTP 200，并加载 `bootstrap.js?v=219`。

## 2026-07-24 权限系统方案

- 已将权限系统正式方案固化到 `docs/permission-system-plan.md`，采用 RBAC 角色权限、数据范围、条件策略和特殊授权的组合模型。
- 文档包含权限分类、编码规范、优先级、数据模型、前后端架构、配置后台、权限模拟器、审计、实时生效、迁移安全及四期实施计划。
- 已完成现有权限盘点并写入 `docs/permission-inventory.md`：确认 `roles.permissions` 和 `/api/auth/permissions` 已存在但未成为实际安全边界，页面和接口仍大量依赖角色字符串及归属硬编码。
- 已冻结首批页面及新建入口权限编码、现有角色验收矩阵和旧冒号权限兼容映射。第一批只接管页面显示和新建入口，后端写接口在并行比对通过后再接管。
- 已新增 `PermissionCatalog` 与 `PermissionService`：统一角色别名、翻译旧冒号权限，并以当前角色行为模板补齐历史权限数据；`GET /api/auth/permissions` 现返回标准角色、兼容模式、权限版本、权限列表和预留数据范围。
- 登录及管理员身份切换都会刷新能力清单；侧边栏、路由进入和三类项目/需求新建入口已改为读取权限编码。能力接口临时失败时保持旧页面可用，后端原有角色、人员归属和业务状态校验仍完整保留。
- 前端缓存版本更新至 `bootstrap.js?v=217`，相关核心和列表模块同步递增；全量前端 JavaScript 语法、差异检查及 Java 21 `clean package` 通过，87 项测试全部通过。本地 `dev` 已重启为 PID `21763`，首页 HTTP 200 并实际加载新模块版本，未登录能力接口按预期返回 401。
- 已新增 `permissions`、`role_permissions`、`role_permission_scopes`、`permission_versions`、`permission_audit_logs` 对应实体与 Repository；能力服务会合并规范化允许、旧权限和兼容模板，并让规范化 `deny` 最终覆盖允许。
- 新增迁移 `2026-07-24-add-permission-foundation.sql`：只创建权限底座表、写入 71 个权限定义、按现有六类角色补默认授权和初始版本，不删除或覆盖 `roles.permissions`。迁移使用 `CREATE TABLE IF NOT EXISTS`、唯一键、`INSERT IGNORE` 及幂等更新。
- 已在一次性 MySQL 8.4.10 容器中连续执行迁移两次，结果稳定为 71 个权限、145 条角色授权、6 个角色版本；临时容器验证后已删除。Java 21 `clean package` 及 88 项测试通过。
- 本地 `dev` 已用新实体重启为 PID `22453`，Hibernate 创建测试环境权限表后应用正常启动；首页 HTTP 200、加载 `bootstrap.js?v=217`，启动日志无应用错误。
- 当前尚未提交、推送或部署；生产发布必须先备份并执行、登记上述迁移，再部署使用新实体的应用。下一步让角色管理后台读取规范化权限、保存授权时递增版本并写入审计。
- 角色管理 API 已切换到 `PermissionManagementService`：权限定义和角色列表读取规范化表；新建、修改、删除角色均要求审计原因，保存全部 71 项显式 `allow/deny`，递增角色版本并记录操作者、来源 IP 及前后 JSON 快照。系统角色不可删除、存在关联用户的角色不可删除。
- 角色管理前端展示规范化权限及高风险标识，保存前要求填写原因；修改当前角色后立即刷新能力清单和侧边栏。缓存更新至 `bootstrap.js?v=218`、`admin-roles.js?v=149`。
- 首次向测试库执行迁移前备份 `/tmp/designpm-permission-before-20260724_171643.sql.gz`（27970 字节，`gzip -t` 通过）。测试库历史键为 `Promotion`、`supply`，据此补充别名感知解析和迁移条件；更新后的迁移在一次性 MySQL 中连续执行两次，并在测试库幂等补齐后均为 71 个权限、145 条授权、6 个版本。
- 全量前端 JavaScript 语法、差异检查和 Java 21 `clean package` 通过，89 项测试全部通过；本地 `dev` 已重启为 PID `23554`，首页 HTTP 200。测试库不存在代码内置的 `admin_liu` 账号，因此未用该账号执行登录态页面验收，权限管理核心保存、deny、版本和审计路径已有自动化测试覆盖。
- `POST /api/projects` 已按项目类型分别要求 `project.channel.create` 或 `project.regular.create`；`POST /api/design-requirements` 要求 `design_requirement.create`。缺少权限时返回 403 及具体权限编码，且不会进入业务保存逻辑；通过权限后仍执行原角色白名单、本人归属、负责人有效性和字段校验。
- 新增项目创建和设计需求创建越权测试，完整构建现为 92 项测试通过；本地 `dev` 已重启为 PID `24351`，首页 HTTP 200、启动日志无应用错误。当前仍仅本地修改，未提交、未推送、未部署。

## 2026-07-24 设计/送审需求交付与评分

- 修复产品推广角色别名未统一造成右上角显示 `promotion`、设计需求列表不显示创建按钮的问题，并递增启动入口和相关模块缓存版本。
- 设计需求创建表单增加交付设计师；后端以真实用户资料校验设计师和企划均为有效在职角色，不信任前端提交的姓名。
- 新增独立设计需求评分记录：设计师交付后先自评；销售创建由销售与企划复评，产品推广创建由产品推广与企划复评，企划创建由企划与管理员复评；两位复评都完成后需求才结束。
- 交付支持成果说明、参考图和附件；重复交付会开启全新评分周期并清除旧分，提前、越权和重复评分均会被拒绝。
- 新增生产迁移 `2026-07-24-add-design-requirement-scoring.sql`，只增加字段、索引和评分表，不删除或改写既有业务数据。
- 完整测试 85 项、全量前端 JavaScript 语法、差异检查和本地页面资源加载通过；本地 `dev` 已运行最新 JAR，`bootstrap.js?v=215` 且控制台无加载错误。用户已明确要求推送仓库和生产，发布流程进行中。
- 上线注意：历史设计需求没有交付设计师和评分记录，无法自动推断人员，本次迁移不擅自回填；新建需求使用完整流程。
- 发布提交 `d2b67823aff0323140fb6f2289128ba36f8abe3c` 已推送并部署生产。发布前数据库确认历史设计需求为 0 条，因此不存在实际旧数据兼容阻塞。
- 生产备份 `/root/emie/db-backup-before-d2b6782-20260724_153416.sql.gz` 已通过 `gzip -t` 和关键表检查；迁移 7 个字段、评分表、3 个索引及外键均验证并登记校验和。
- 生产 `release-sha.txt`、应用 JAR SHA、容器、接口、前端资源版本和启动日志均验证通过；回滚 JAR 为 `/root/emie/app.jar.bak.pre-d2b6782-20260724_153506`，未执行回滚。
- 发布后发现设计需求列表忽略后端 `statusLabel`，将内部状态 `draft` 按通用项目字典显示为“草稿”；已改为列表优先采用接口业务状态文案，并递增 `dashboard-projects.js` 与启动入口缓存版本。该问题只影响显示，不影响需求流程或数据库状态。
- 状态文案修复提交 `0abd63a16ed4fa37a2ca6f1e516d9cba3a75eda7` 已推送并部署生产；生产加载 `bootstrap.js?v=216` → `dashboard-projects.js?v=159`，应用与预览容器 running、重启次数 0，首页 HTTP 200，近期严重日志 0。
- 定位管理员身份切换器中“产品推广”无徽章的原因：切换器已生成 `r-promotion` 类，但全局样式只配置了其他五类角色。现补充紫色产品推广徽章并将 `app.css` 缓存版本递增至 `v29`。
- 功能提交 `66346838f8183c5f5a276d3b2fbd6955e396c375` 已推送并部署生产；生产 `release-sha.txt`、JAR SHA、CSS 版本和产品推广徽章规则均验证一致，首页、公开配置及预览健康接口 HTTP 200，两个容器 running、重启次数 0。回滚包为 `/root/emie/app.jar.bak.pre-6634683-20260724_161457`，未执行回滚。

## 2026-07-23 管理员身份切换动态角色

- 根因是 `core-identity.js` 的角色顺序和标签只硬编码了销售、企划、设计师、供应链、管理员，因此后端已经返回的 `promotion` 用户仍被循环忽略，自定义角色也同样无法出现。
- 身份切换器现并行读取 `/api/users` 与轻量角色字典 `/api/users/roles`，用角色管理的显示名称动态分组，并保留内置顺序；该字典不暴露权限配置，管理员切换到其他用户视角后仍可读取。补充 `promotion`/历史别名规范化和按用户 ID 去重。
- 新增、编辑、删除角色后立即刷新身份切换器；用户新增、资料/角色/状态修改和删除原有刷新链路继续生效。以后新增角色和用户无需修改固定前端列表。
- 补充产品推广头像、分组颜色和全局角色标签，递增相关模块、样式和启动入口缓存版本。
- 完整构建、77 项测试、前端语法及差异检查通过；本地 `dev` 已重启为 PID `54403`，健康接口 HTTP 200，实际加载 `app.css?v=28`、`bootstrap.js?v=203`、`core-identity.js?v=148`。当前仅本地修改，未推送、未更新生产。

## 2026-07-23 项目详情双层进度

- 用户确认五阶段属于整个项目共用的一条子任务总流程，而不是每个子任务分别经历。流程固定为：设计 → 设计送审 → 打样送审 → 产品宣发 → 大货。
- 新增项目级 `workflowStage` / `workflowStatus` 和不可覆盖的 `project_workflow_attempts` 审核轮次表；设计送审、打样送审、产品宣发支持任意多轮提交、通过和驳回，每轮保留提交人、审核人、时间、意见。
- 产品企划负责人推进设计/大货执行阶段并发起或重新发起送审；渠道定制由项目对应销售审核，公司常规品由管理员审核。暂停、终止或待终止项目禁止推进。
- 项目详情使用与项目总进度一致的五节点管道；当前节点橙色、驳回红色、完成绿色，节点摘要显示当前轮次和累计驳回次数，详细历史默认折叠。
- 原“项目进度”管道改名为“项目总进度”，继续表达创建、企划接单、子任务执行、项目完结的生命周期。
- 新增幂等生产迁移 `2026-07-23-add-project-workflow.sql`；历史已完成项目回填为流程完成，其余项目从设计阶段开始。已递增项目详情模块和启动入口缓存版本；当前未推送、未更新生产。
- 完整构建、79 项测试、全量前端 JavaScript 语法及差异检查通过；本地 `dev` 已重启为 PID `58496`，应用启动日志正常，健康接口 HTTP 200，实际加载 `bootstrap.js?v=204` → `project-detail.js?v=174`。

## 2026-07-23 后台群发服务启动修正

- 本地重启验证发现已推送但未部署生产的 `NotificationBroadcastJobService` 同时存在生产构造器和测试构造器，Spring 无法自动选择，导致应用上下文启动失败。
- 已对生产构造器增加明确依赖注入标记；生产仍运行 `961cd1b`，从未部署包含该问题的后台群发版本，因此线上服务未受影响。
- 修正后完整测试通过，本地应用上下文真实启动成功并通过 HTTP 健康检查。

## 2026-07-23 管理员添加子任务权限修正

- 用户明确管理员不能添加子任务。此前前端为修复“非负责人企划误显示按钮”时，错误地把管理员加入 `canManageSubTasks`，与后端“仅项目负责人企划可创建”的既有权限不一致。
- 已改为仅当前项目负责人产品企划显示“添加子任务”按钮，管理员不再显示；后端权限无需调整。
- 已递增项目详情模块和启动入口缓存版本；76 项测试、前端语法及差异检查通过。当前仅本地修改，未推送、未更新生产。

## 2026-07-23 系统更新通知页面误报失败诊断

- 用户在生产点击全员通知后页面提示错误。生产日志显示 `POST /api/admin/notifications/temporary-broadcast` 实际以 HTTP 200 完成，但串行发送 27 人耗时约 26.5 秒。
- 前端全局请求超时固定为 20 秒，先于后端完成而主动中断连接，因此页面显示“请求超时/发送失败”，后端随后出现客户端断开的连接重置警告。
- 生产数据库核验本次生成 27 条系统更新通知和 27 条飞书投递，状态全部为 `delivered`；失败 0、未绑定 0。不得再次点击补发，否则会造成 27 人重复收到通知。
- 已在本地将全员广播改为单线程后台任务：POST 立即返回任务编号，页面每秒轮询状态并显示最终统计；同一时间禁止重复创建群发任务。该实现不会用延长超时掩盖问题，并能随用户数量增长保持 HTTP 请求快速返回。
- 完整构建、76 项测试、全量前端 JavaScript 语法及差异检查通过；测试使用模拟飞书服务，未再次触发真实通知。当前修复待提交并仅推送仓库，不部署生产。

## 2026-07-23 仓库与生产发布状态

- 发布提交 `961cd1b` 已推送到 Gitee `project_manager_system`，本地和远端完整 SHA 一致；75 项测试、精确提交干净构建、全量前端 JavaScript 语法及差异检查通过。
- 生产发布前确认旧 `release-sha.txt` 仍记录 `efed402`，但旧 JAR 更新时间更晚；本次没有依赖过期标记回滚，而是对实际旧 JAR 单独生成回滚包。
- 生产数据库备份已通过 `gzip -t`，`design_requirements` 幂等迁移执行后验证 1 张表、18 个字段；应用 JAR 上传前后 SHA-256 一致。
- 首次 Docker BuildKit 构建因生产目录缺少 `.dockerignore`、发送持久化文件上下文而卡住，旧容器未中断；终止无效构建后增加忽略规则，最终短暂停止应用释放内存并用传统构建器完成镜像重建。忽略规则已补入仓库，后续构建不会再包含上传、日志和备份。
- 生产 `release-sha.txt=961cd1bd8f0939e89f00b5483e281831badca1f6`；应用和预览容器运行，公开配置、预览健康接口 HTTP 200，受保护项目接口未登录返回 401，前端加载 `bootstrap.js?v=199` → `admin-shell.js?v=149`，启动日志无应用错误。发布成功，未执行回滚。

## 2026-07-23 系统更新飞书通知入口

- 复用现有管理员全员飞书群发后端能力，将管理后台入口由“临时飞书通知”明确升级为“系统更新通知”，避免建设重复的发送链路。
- 页面明确说明收件范围为除已停用账号外的所有系统用户；未绑定飞书的用户无法投递，并会在发送结果中单独统计。
- 按钮调整为“发送给全部用户”，群发前增加二次确认，降低误触造成全员通知的风险；发布和验证过程中未触发真实通知。
- 前端语法检查、74 项全量测试和差异检查通过；本地 `dev` 已重启为 PID `31537`，健康接口 HTTP 200，实际返回缓存链为 `bootstrap.js?v=199` → `admin-shell.js?v=149`。

## 2026-07-23 本地添加子任务报错诊断

- 本地日志确认添加子任务请求在 `ProjectService#addSubTask` 被权限校验拒绝，错误为“仅项目负责人企划可创建子任务”；数据库连接和应用运行正常。
- 根因是项目详情前端仅按“当前角色为企划 + 项目状态允许”展示添加按钮，没有同时校验 `detail.plannerId === currentUserId`。部门负责人或其他具备项目可见范围的企划因此能看到按钮，但后端仍正确拒绝对非本人负责项目创建子任务。
- `POST /api/projects/{id}/tasks` 控制器未像编辑、交付等接口一样捕获业务 `RuntimeException` 并返回 400，导致权限拒绝表现为服务端异常。当前仅完成诊断，尚未修改。
- 已完成修复：前端仅对项目负责人企划或管理员展示“添加子任务”按钮；后端捕获业务异常并返回 400 + 明确错误信息。已补前端权限契约和控制器错误响应测试，并递增项目详情模块与入口缓存版本。
- 专项测试、完整构建、74 项全量测试、全部前端 JavaScript 语法和差异检查通过；本地 `dev` 已重启为 PID `29054`，健康接口 HTTP 200，缓存链为 `bootstrap.js?v=198` → `project-detail.js?v=171`。未提交、未推送、未部署生产。

## 2026-07-23 评分中心驳回排除与排序修正

- 评分聚合服务直接排除状态为 `rejected` 的子任务，避免其被错误归入“已评分”；负责人重新交付后任务回到 `delivered`，才会按重置后的审核状态重新进入评分流程。
- 评分页面排序改为先按待评分/已评分分组，待评分始终置顶；每个分组内再按计划完成时间升序排列，无日期任务排在组内最后。
- 新增后端回归测试验证驳回任务被排除、正常待评分任务保留，并补前端排序顺序断言。本次仅本地修改，未提交、未推送、未部署生产。
- 专项测试、完整构建、73 项全量测试、全部前端 JavaScript 语法和差异检查通过；本地 `dev` 已重启为 PID `26488`，健康接口 HTTP 200，缓存链为 `bootstrap.js?v=197` → `dashboard-scoring.js?v=160`。

## 2026-07-23 评分中心增加产品企划筛选

- 评分聚合接口新增 `plannerId`、`plannerName`，并同步修正项目名称优先使用 `productName`、历史数据回退到需求说明。
- 评分页面新增“产品企划”筛选卡，按稳定用户 ID 过滤；搜索和卡片摘要同时支持产品企划名称。
- 已递增评分模块和最外层入口缓存版本；完整构建、72 项测试、全部前端 JavaScript 语法和差异检查通过。本地 `dev` 已重启为 PID `22894`，健康接口 HTTP 200，服务确认返回 `bootstrap.js?v=195` → `dashboard-scoring.js?v=158`。本次仅本地修改，未提交、未推送、未部署生产。
- 用户反馈“全部产品企划”选项缺人，确认原实现错误地只从当前评分结果生成选项；已改为读取系统用户缓存中的完整 `planner` 分组，没有当前评分任务的企划也会出现在下拉框中。
- 修正后完整构建、72 项测试、前端 JavaScript 语法和差异检查通过；本地 `dev` 已重启为 PID `24131`，健康接口 HTTP 200，缓存链为 `bootstrap.js?v=196` → `dashboard-scoring.js?v=159`。

## 2026-07-23 我的子任务显示所属项目名称

- 修复 `/api/projects/my-subtasks` 与部门子任务接口误将 `productRequirements` 作为 `projectName` 返回的问题；现在优先返回正式 `productName`，仅对没有产品名称的历史项目回退到需求说明。
- 将卡片内原有的弱提示改为独立“项目上下文条”：项目名为主信息，项目类型和编号为辅助信息，整块可点击进入项目，并补充桌面端 hover/focus 与移动端两行适配。
- 新增接口回归测试，覆盖正常项目名称和历史数据回退；专项测试、完整构建、72 项全量测试及全部前端 JavaScript 语法检查通过。
- 本地 `dev` 已用新 JAR 重启，PID `16050` 正在监听 `8080`，公开配置接口 HTTP 200，启动日志无应用级 `ERROR`。本次仅本地修改，未提交、未推送、未部署生产。
- 根据用户反馈，移除独立项目上下文条，改为在子任务名称后直接显示“（所属项目：项目名）”；项目跳转继续使用卡片底部“查看项目”按钮，CSS 与模块缓存版本已递增。
- 调整后完整构建、72 项测试、前端 JavaScript 语法及差异检查通过；本地 `dev` 已重启为 PID `20110`，健康接口 HTTP 200。
- 首次反馈页面仍为旧样式，确认为外层 `bootstrap.js` 版本未递增导致浏览器继续复用旧模块依赖图；入口版本已从 `v193` 更新为 `v194`，确保新 `dashboard-designer.js?v=174` 被加载。

## 2026-07-23 新设备主力开发环境接管

- 当前主工作区已迁移至 `/Users/emie/Documents/emie`；后续本地开发以该目录为准，旧设备路径仅作为历史记录保留。
- 已补充 `design_requirements` 幂等生产迁移，并新增设计需求创建权限、负责人防伪、列表可见范围，以及临时广播输入与通道开关测试。
- 已修正开发脚本的进程识别：使用当前项目 JAR 的绝对路径记录 PID，健康检查必须同时命中新进程；`restart` 不再终止占用 `8080` 的无关进程。
- 新设备未预装 Maven，原 Wrapper 使用的 3.9.6 主站与镜像地址已失效；已增加 Apache 官方归档地址作为回退，并改用跨平台文件大小检查。
- Maven 3.9.6 与项目依赖已在新设备初始化完成；`scripts/mvnw-java21.sh clean package` 成功，70 项测试全部通过（0 失败、0 错误、0 跳过），全部前端 JavaScript 模块语法检查通过。
- 已用修正后的脚本完成一次重启闭环：PID 文件、Java 进程、项目工作目录和 `8080` 监听均指向 PID `11385`，独立 `emie-dev` screen 会话正常，公开配置接口 HTTP 200，启动日志无应用级 `ERROR`。
- 当前本地 `dev` 服务保持运行，飞书同步消费者沿用 `dev` profile 默认关闭。本轮未提交、未推送、未执行生产迁移、未部署生产；开发环境 `ddl-auto=update` 已按实体检查共享测试库结构。

## 2026-07-23 迁移前暂停本地项目

- 用户要求先暂停项目；已确认 `8080` 的监听进程工作目录为 `/Users/jinli/Documents/emie` 且运行本项目 JAR，再向该精确 PID 发送 `TERM`。
- 进程在停止宽限期结束后退出，`emie-dev` screen 会话已关闭；`8080` 当前无监听，公开配置健康接口不可访问，符合暂停状态。
- 本次只停止本机开发服务，没有修改生产环境、共享测试数据库或飞书配置；未提交、未推送、未部署。
- 新设备恢复完成前保持旧工作区和加密迁移包，不要删除 `.env`、`uploads/`、`data/` 或 `backups/`。

## 2026-07-23 设备迁移交接基线

- 已新增 [`device-migration-handoff.md`](device-migration-handoff.md)，记录当前提交、远端同步状态、环境要求、本机数据清单、加密迁移方法和新设备验收步骤。
- 当前业务分支为 `project_manager_system`，本地与远端均为 `c1ed377f381feeec25fdb3fc666f93b17e0a9ed9`，迁移盘点前工作区干净、远端差异为 `0 / 0`。
- Java 21 全量构建成功，65 项测试全部通过；全部前端 JavaScript 模块语法检查通过。本机 `dev` 服务继续在 `8080` 运行，公开配置接口 HTTP 200。
- 本机需另行安全迁移 `.env`、按需迁移 `.server.local.env`，并保留 `uploads/`、`data/`、`backups/`；`target/` 和本地工具缓存可在新设备重建或丢弃。
- 最新提交新增独立设计/送审需求及临时飞书通知，但缺少 `design_requirements` 生产数据库迁移和专项回归测试；生产使用 `ddl-auto=validate`，补齐迁移前不得直接部署。
- 迁移整理期间发现启动命令含 JVM 参数，而 PID 检测仍按连续的 `java -jar` 匹配，因此不会生成 PID 文件；`restart` 会空输出退出，直接 `start` 还可能遗留旧进程并误读旧健康接口。本次已验证旧 PID 工作目录、只终止该 EMIE 进程，再启动最新 JAR；当前新 `screen` 会话、Java 进程、启动日志和 HTTP 200 一致，后续需修正启停脚本。
- 仓库可确认的最近生产代码记录为 `efed402`。`c1ed377` 已在远端但没有对应生产发布记录，本次未登录生产核对，实际生产 SHA 未确认。
- 本次仅整理本地文档，未提交、未推送、未部署；迁移时必须复制这些本地文档，或在用户重新授权后另行处理版本控制。

## 2026-07-21 AI 文件选择器兼容修复

- 提交 `efed402` 已推送 Gitee 并部署生产。
- 图片/参考图的浏览器原生选择器已允许 `.ai`，并递增入口及模块资源版本，避免旧缓存继续拦截。
- 生产 JAR SHA-256：`74f3c4924e6a5c34122c0623680bc63228d2e3ba8b0230b9056c2a8d69afedbe`；应用容器正常，公开配置接口 HTTP 200。
- 发布前数据库备份：`/root/emie/db-backup-before-e9936d3-20260721_171639.sql.gz`；回滚包：`/root/emie/app.jar.bak.pre-e14d215-20260721_171639`。

## 2026-07-21 AI 文件上传兼容修复

- 提交 `209c158` 已推送 Gitee 并部署生产。
- `.ai` 已加入图片区域和附件区域的后端白名单；按源文件保存，不参与缩略图和在线预览。
- 生产数据库备份 `/root/emie/db-backup-before-87a8a2e-20260721_170013.sql.gz` 已通过 `gzip -t`，应用容器正常，公开配置接口 HTTP 200。

## 2026-07-21 最新生产发布

- 提交 `5f7232c` 已推送 Gitee 并部署生产。
- 生产数据库备份 `/root/emie/db-backup-before-442854b-20260721_163504.sql.gz` 已生成并通过 `gzip -t`；旧 JAR 回滚包已保留。
- 本次包含项目负责人两级筛选、企划统一项目视角、子任务权限调整、销售接收企划子任务、`.ai` 附件支持及缩略图内存优化。
- 生产 `release-sha.txt` 已记录 `5f7232c`，应用容器启动成功，公开配置接口 HTTP 200，生产 JVM 已调整为 `-Xmx320m`。

## 2026-07-18 高优先级优化进展

- 已优化 `AdminService#getWorkloadStats`：销售、企划、设计师/供应链统计改为按负责人一次性 `GROUP BY` 聚合，消除按用户逐个查库的 N+1。
- 已优化 `NotificationRetryService#recentFailures`：失败投递对应通知改为批量按 ID 查询，消除逐条查询通知的 N+1。
- 新增 `db/migrations/README.md`，明确迁移备份、幂等、版本记录和禁止修改已执行历史脚本的规则；本次未改写生产历史迁移 SQL。
- 验证：`git diff --check` 通过，`./mvnw test -q` 已执行通过，本地 `./scripts/dev.sh restart` 完成，`http://localhost:8080/api/admin/public-config` 已就绪。
- 当前状态：以上改动仅在本地工作区，未提交、未推送、未部署生产；下一步可继续优化工作量时间线接口和运行时告警闭环。

## 2026-07-18 第二批高优先级优化进展

- `AdminService#getWorkloadTimeline` 已改为按负责人批量聚合创建/完成数量，避免每个员工执行多次统计 SQL。
- `RuntimeAlertService` 已增加按告警类型的冷却去重，默认冷却 10 分钟；JVM、飞书同步队列和数据库连接池异常不再每分钟重复刷相同日志。
- 验证：`git diff --check` 通过，`./mvnw test -q` 已执行通过；本地服务已重新打包并重启，`http://localhost:8080/api/admin/public-config` 返回 HTTP 200。
- 当前状态：本批改动仍仅在本地工作区，未提交、未推送、未部署生产。

## 2026-07-18 第三批优化进展

- 通知重试任务已改为批量加载本批次通知和收件人用户，避免每条失败投递分别查询通知和用户。
- 验证：`git diff --check` 与 `./mvnw test -q` 通过；本地服务已重新打包重启，`http://localhost:8080/api/admin/public-config` 返回 HTTP 200。
- 当前状态：改动仅在本地工作区，未提交、未推送、未部署生产。

## 2026-07-18 第四批优化进展

- 新增 `GET /api/projects/my-tasks`，执行角色工作台一次性读取可见项目及子任务；设计师工作台不再先获取项目列表、再并发逐项目请求详情，减少请求瀑布和重复权限查询。
- 前端工作台已切换到该聚合接口，并同步更新静态资源版本；模块布局测试断言已同步调整。
- 验证：`git diff --check`、`./mvnw test -q`、全部前端 JS 语法检查通过；本地服务已重启并返回 HTTP 200。
- 当前状态：本批改动仅在本地，未提交、未推送、未部署生产。

## 2026-07-18 第五批优化进展

- 聚合工作台接口的评分记录已改为对全部任务一次性批量查询，再按任务 ID 在内存中分组；避免项目详情转换过程中按项目重复访问评分表。
- 验证：`git diff --check`、`./mvnw test -q`、全部前端 JS 语法检查通过；本地服务已重新打包重启并返回 HTTP 200。
- 当前状态：本批改动仅在本地，未提交、未推送、未部署生产。

## 2026-07-18 高优先级 1-4 优化进展

- 工作台聚合接口不再加载项目操作日志；详情页仍保留完整日志，避免工作台响应随审计历史增长。
- 工作量时间线汇总由四次独立总计查询合并为项目/任务各一次条件聚合查询，负责人统计继续使用批量 `GROUP BY`。
- 新增 `runtime_alerts` 告警状态表：记录告警首次出现、最近出现、当前 active/recovered 状态和恢复时间；原有日志冷却机制保留，新增恢复日志。
- 新增 `schema_migrations` 基础表和迁移登记规范；不改写、不伪造历史迁移记录，后续新迁移可按版本与校验和登记。完整的自动迁移执行器仍需单独引入 Flyway/Liquibase 或发布脚本实现。
- 新增数据库迁移：`2026-07-18-add-runtime-alerts.sql`、`2026-07-18-add-schema-migrations.sql`，均使用 `CREATE TABLE IF NOT EXISTS`。
- 验证：`git diff --check`、`./mvnw test -q`、全部前端 JS 语法检查通过；本地服务已重新打包重启并返回 HTTP 200。
- 当前状态：本批改动仅在本地，未提交、未推送、未部署生产；生产迁移必须先备份并按发布手册执行。

## 2026-07-18 优先级收敛结论

- 当前核心性能、N+1 查询、工作台请求、运行时告警和迁移基础防护已达到本阶段目标。
- Flyway/Liquibase、多实例锁、告警外送、服务拆分、慢查询监控等统一归为中长期架构优化，不再标记为当前高优先级。
- 后续只有出现真实生产故障、明显性能回退或新的数据安全风险时，才重新提升相关事项优先级。

## 2026-07-18 生产发布结果

- 高优先级优化提交 `9e6f6262d3e6cabfbadf9ceb06edbade829eb186` 已推送 Gitee 并部署生产。
- 发布前数据库备份 `/root/emie/db-backup-before-9e6f626-20260718_172338.sql.gz` 已通过 `gzip -t`；旧应用回滚包已保留。
- 运行时迁移表 `runtime_alerts`、`schema_migrations` 已创建并核验；生产 `release-sha.txt` 与目标提交一致。
- `emie-app` 与 `emie-preview-converter` 正常运行；公开配置和预览健康接口 HTTP 200，应用启动日志无异常。
- 生产部署已完成，当前没有待回滚事项。

## 2026-07-18 草稿筛选项隐藏

- 已从全部项目、渠道定制单、公司常规品和子任务管理的状态筛选中移除“草稿”选项。
- 仅隐藏筛选入口，草稿保存、历史数据和后端 `draft` 状态兼容逻辑均保留。
- 前端资源版本更新至 `bootstrap.js?v=151`，本地服务已重启并验证 HTTP 200。
- `./mvnw test -q`、全部前端 JS 语法检查和 `git diff --check` 通过；当前改动未提交、未推送、未部署生产。

## 2026-07-18 我的子任务二级菜单（本地）

- 左侧“我的子任务”增加“全部子任务 / 待处理子任务 / 已完成子任务”二级菜单。
- 顶部工作台卡片保持不变；子任务页按逾期、驳回、待审核、截止日期、更新时间排序。
- 待处理包含未完成任务，已完成包含 `approved/completed`；草稿保存和后端草稿状态不受影响。
- 前端资源版本更新至 `bootstrap.js?v=152`、`app.css?v=23`；`./mvnw test -q`、全部 JS 语法检查、`git diff --check` 通过，本地服务 HTTP 200。
- 当前改动仅在本地，未提交、未推送、未部署生产。

## 2026-07-18 我的子任务显示修正（本地）

- 修正“我的子任务”在管理员等角色下显示为项目容器的问题：管理员也进入统一任务卡片视图，项目只作为任务归属信息展示。
- `/api/projects/my-tasks` 放开管理员只读访问；管理员任务页展示全部子任务，设计师/供应链/企划仍按本人任务和待认领规则过滤。
- Java 测试、全部 JS 语法检查、`git diff --check` 通过；本地服务已重启并 HTTP 200。
- 当前改动未提交、未推送、未部署生产。

## 2026-07-18 我的子任务独立查询（本地）

- 已确认现有 `sub_tasks` 表可复用，不新增任务表；新增 `publisher_id/publisher_name/publisher_role`，建立负责人或发布人关联。
- 新增 `GET /api/projects/my-subtasks`，返回当前用户负责或发布的子任务；页面不再通过项目详情反推任务。
- 产品企划的“全部子任务”包含自己负责和自己发布的任务；待处理按未完成任务，已完成按 `approved/completed`。
- 新迁移 `2026-07-18-add-my-subtask-publisher.sql` 可重复执行，并为历史任务按项目企划负责人回填发布人关系。
- Java 测试、JS 语法检查、`git diff --check` 通过；本地服务已重启并 HTTP 200。
- 当前改动仅在本地，未提交、未推送、未部署生产。

## 2026-07-20 测试库来源确认

- 本地 `dev` profile 连接远程共享测试 MySQL 的 `designpmtest` 数据库，不是生产库，也不是生产到测试的实时镜像。
- 项目内未发现生产库到测试库的定时同步、备份恢复任务或数据库复制配置；测试库数据只能来自历史人工导入、测试操作或独立维护。
- 本地飞书同步消费者默认关闭，不能将“本地同步到飞书”误认为“生产库同步到测试库”。

## 2026-07-20 子任务评分后详情刷新修复（本地）

- 根因：在“我的子任务”页面完成交付/评分后，`refreshAfterMutation` 只刷新当前列表和徽章，没有刷新仍打开的项目详情弹窗。
- 修复：统一在任意任务变更后，只要详情弹窗存在就重新请求 `/api/projects/{id}` 并重绘详情内容和操作区。
- 前端版本更新至 `bootstrap.js?v=153`；Java 测试、全部 JS 语法检查、`git diff --check` 通过，本地服务已重启并 HTTP 200。
- 当前改动未提交、未推送、未部署生产。

## 2026-07-20 测试库子任务发布人回填

- 已在本地共享测试库执行 `2026-07-18-add-my-subtask-publisher.sql`，为历史子任务补齐发布人关系和索引。
- 回填后测试库 `sub_tasks` 共 6 条已具备发布人关系；吴思欣（`feishu_ou_4526d`）关联子任务共 6 条，其中已完成 1 条、待处理 5 条。
- 本地服务接口 HTTP 200；本次只操作测试库，未操作生产库，代码改动仍未提交、未推送、未部署。

> 本文档是本地开发任务的实时交接入口。每次开发进度、诊断结论、验证结果或运行状态变化后，必须在结束当前任务前更新本文档。不得记录密码、token、secret、cookie 或完整敏感配置。

## 1. 当前快照

| 项目 | 当前状态 |
|---|---|
| 最后更新 | `2026-07-17 00:16 +0800` |
| 业务分支 | `project_manager_system` |
| 应用发布提交 | `62502d7` |
| 代码状态 | 左侧导航统计修复已推送并发布生产；浏览器登录后的统计稳定性待用户验收 |
| 本地服务 | `dev` profile，测试库，`8080` 端口已覆盖为导航统计修复版本，前端资源 `v146` |
| 运行进程 | Java PID `84883`（仅为当前快照，重启后会变化） |
| Java 基线 | 当前业务分支的 `pom.xml` 与 Docker 镜像已改为 Java 21；本机 Temurin 21.0.11，生产仍为 Java 17 |
| 本机入口 | `http://127.0.0.1:8080` |
| 局域网入口 | `http://192.168.200.82:8080`（IP 可能随网络变化） |

- 已按用户确认移除 4 份废弃项目导入 Excel 模板。本次为独立本地维护提交，不推送、不部署；填写说明、参考数据和结构检查记录仍保留。
- 本地已实现 Excel 项目批量导入的管理员接口与受显式文件路径保护的命令行入口：完整预校验通过后才原子写入，自动映射数值零售价到已启用价格区间，保留项目日志与同步记录但抑制批量通知。
- 用户已确认统一导入映射：全部项目写为一级 IP“环球”、二级 IP“小黄人”。导入命令在完整预校验通过后已自动补齐该测试库 IP 配置并成功导入 27 条项目（渠道定制单 16 条、公司常规品 11 条）；价格数值已映射到现有价格区间，项目保留操作日志和同步记录但未发送批量通知。当前本地应用已覆盖为最新包，页面接口 HTTP 200。
- 生产导入已完成：发布提交 `8db9d0d` 后，已生成并校验生产数据库备份；项目从 3 条增至 30 条，新增渠道定制单 16 条、公司常规品 11 条。新增 27 条均为 `环球 / 小黄人`，数值价格已映射为系统价格区间，未批量发送通知；同步队列无待处理项，应用/预览容器正常且公开配置 HTTP 200。
- Java 21 已在 Docker 构建镜像、生产运行镜像和 Maven 编译基线生效；本地默认终端仍可能是 Java 17，因此新增 Java 21 统一环境脚本与 `scripts/mvnw-java21.sh`。`dev.sh` 已固定使用 Java 21，Maven Enforcer 会拒绝非 Java 21 构建；同时修复了过期 PID 文件导致本地旧服务未被停止的问题。
- 本地 `dev` 服务改为独立 `screen` 会话 `emie-dev` 守护运行，避免 Codex/终端会话中止时误杀应用；当前 Java PID `26327`，`8080` 的公开配置接口 HTTP 200。停止与重启脚本会同时清理该会话。
- 本地新增已创建项目编辑能力：项目详情的“项目信息”标题右侧仅在渠道定制项目的所属销售、或公司常规品的所属产品企划登录时显示“编辑项目信息”。后端 `PUT /api/projects/{id}` 再次执行同一严格归属校验，管理员不具备绕过权限；可修改产品资料、IP/二级 IP、市场、合规、价格、日期、说明、参考图和附件，不能修改项目类型或销售/企划归属。每次保存记录变更前后快照与变更字段到项目操作日志。已执行全部前端 JS 语法检查和 Java 21 `clean package`（63 测试通过）；入口位置调整后，前端 JS 语法检查与模块布局测试 5/5 通过。本地 `screen` 会话 `emie-dev` 已重启为 PID `41165`，公开配置接口 HTTP 200。当前工作区有该功能的未提交改动，未推送、未部署。
- 本地新增项目列表分页与紧凑布局：全部项目、渠道定制单、公司常规品默认调用 `GET /api/projects/page`，固定每页 15 条；后端按当前角色可见范围、项目类型和创建时间分页，避免列表进入时拉取完整项目表。分页评分汇总通过批量预取评分子任务完成，避免逐项目懒加载子任务的 N+1 查询。筛选首次使用时才按需读取该列表范围的完整数据，再继续按 15 条翻页；首次加载、筛选、翻页均显示“正在查询项目”动画，分页栏显示总页数并支持输入页码跳转。新增项目销售/企划/类型/创建时间复合索引，以及执行人角色/负责人/状态复合索引；生产发布前必须先执行 [`2026-07-16-add-project-list-page-indexes.sql`](../db/migrations/2026-07-16-add-project-list-page-indexes.sql)。项目列表合并为紧凑信息列，降低一屏内的横向和纵向滚动。Java 21 `clean package` 64/64 测试、全部前端 JS 语法检查通过；本地 `screen` 会话 `emie-dev` 已覆盖为 PID `50228`，公开配置接口 HTTP 200；分页接口在未登录状态返回预期的 HTTP 401。本次及此前编辑功能均未提交、未推送、未部署。
- 数据层诊断（测试库）：当前仅 28 个项目、6 个子任务、0 条评分记录，数据量本身不构成慢查询。`EXPLAIN` 证实按 `type + created_at DESC` 的项目分页原先使用 filesort；已补 `idx_project_type_created(type, created_at)`，本地更新后执行计划已显示 Backward index scan / Using index。分页项目查询同时以 EntityGraph 预取产品类目，减少远程测试库的关联查询往返；完整 Java 21 构建 64/64 通过，本地 `emie-dev` PID `53241` / HTTP 200。生产已在有效数据库备份后执行索引迁移，`emie-app` 与预览服务、公开配置和预览健康接口均验证 HTTP 200，运行提交为 `b6406fc7563849f4e9eb76e650f50ddf52b4187d`；生产浏览器登录后的交互验收待用户确认。详细记录见 [`release-records.md`](release-records.md)。
- 项目查询优化第二阶段：`GET /api/projects/page` 现接受 `status`、`category`、`market`、`keyword`、`deadlineStart`、`deadlineEnd`，统一返回 `{items,page,size,total,totalPages}`，非法参数返回 `{code,message}`。列表前端不再回退全量 `GET /api/projects`，筛选/翻页均保持服务端分页。新增 Criteria 动态查询仓储片段，内容和 count 查询共用权限与筛选谓词；旧全量接口保留给未迁移页面兼容。新增索引迁移 [`2026-07-16-add-project-search-indexes.sql`](../db/migrations/2026-07-16-add-project-search-indexes.sql)，并已按发布手册在生产备份后执行。`docs/api-crud-standards.md` 已固化 CRUD 响应、权限、筛选、索引与兼容 SOP。Java 21 `clean package` 64/64、全量 JS 语法检查、`git diff --check` 均通过。
- 项目查询优化第二阶段已发布生产：提交 `bc396bf` 已推送至 Gitee，生产已在有效数据库备份后创建三条筛选组合索引，应用 JAR 哈希与本地一致，`emie-app` 已重建、预览容器持续运行。公开配置/预览健康检查 HTTP 200，未登录分页接口 401，首页已加载 `bootstrap.js?v=145`；本次启动日志无应用错误。实际登录后的关键词和条件筛选交互仍待用户验收。
- 左侧导航蓝色统计圈已发布生产：此前工作台、普通页面渲染和项目操作各自写入徽章，且项目操作调用的接口没有总项目/类型字段，前端将缺失值覆盖为 `0`。现已收敛为服务端 `GET /api/projects/badge-stats` 的唯一统计口径，接口从会话取得身份，项目/类型统计与导航页权限范围一致，待评分统计与评分中心一致；前端仅由 `refreshNavigationBadges` 写入，并在侧栏重建和业务操作后刷新。历史工作台入口改为转发统一函数，移除逐项目详情 N+1 统计。生产已确认 `v146`、公开配置 200、未登录徽章接口 401，实际登录后的切页和操作稳定性待用户验收。

## 2. 当前交接重点（下一位 AI 先读）

### 通知中心（新需求）

- 已确认通知必须覆盖完整项目生命周期，不能因合并、摘要或限流漏掉必达事件；站内通知为必达主通道，飞书为可靠外送通道。
- 设计必须覆盖项目创建/归属、子任务派发/改派/交付/驳回/再次交付、两级审核、临期逾期、权限组织、系统失败，以及项目和子任务的人工催办。
- 设计文档见 [`notification-system-design.md`](notification-system-design.md)，其中定义了 Outbox、站内通知、渠道投递、追加式审计、飞书卡片、幂等和催办规则。
- 第一阶段基础已发布生产：新增通知领域实体、数据库迁移、可靠 Outbox、站内通知/渠道投递/审计仓库和幂等回归测试；系统配置已新增“通知中心”策略组。
- “保存并发送测试”已提交、推送并部署生产（`5d00f65`）：先保存通知策略，再向当前管理员生成站内测试通知；当飞书渠道开启且该账号已绑定 Open ID 时，调用飞书消息 API 发送交互式卡片。每次测试都写入事件、投递和审计记录，飞书未启用、未绑定或失败会如实返回状态，绝不伪造成功。
- 飞书实际测试返回 `230099/200621` 后已定位为卡片 JSON 2.0 的 `body` 被错误序列化为数组。已修为 `body.elements` 对象结构并加入结构回归测试；Java 21 完整构建 58 项测试通过，本地 PID `43853` / `8080` HTTP 200。热修复 `e907f4c` 已推送并发布生产，生产应用与预览容器、公开配置和 `v133` 页面验证通过。
- `NotificationTemplateService` 已集中生成项目指派、任务派发/改派/接单/交付/驳回/再次交付、审核、催办、临期/逾期和系统告警的站内文案与 Card JSON 2.0；模板回归测试后完整构建 60 项测试通过。提交 `233a40b` 已推送并发布生产；当前尚未接入自动通知编排器，因此不会自动发送业务通知。
- 本地已开始业务接入：渠道项目指派、子任务派发、首次交付、再次交付和驳回会创建站内通知、飞书投递与审计；60 项测试通过，screen 会话 `emie-dev` / Java PID `61468` 的 `8080` 已验证 HTTP 200。该接入尚未提交或部署生产；二审流转和催办为下一段工作。
- 已修复子任务派发的通知聚合 ID 在保存前为 `null` 的问题：改为使用持久化后的子任务 ID；本地实际派发已验证站内与飞书投递均为 `delivered`。修复已随 `ccd4b94` 推送，并已在生产应用 `c6f3be3` 部署验证。
- 已修复飞书卡片“查看并处理”无反应的根因：飞书只接受带 `http://` 或 `https://` 的绝对地址，旧卡片传入了 `/`。新增通知中心配置 `notification.publicBaseUrl`，新卡片会生成 `/?projectId=...&taskId=...` 的绝对地址；前端登录后自动打开对应项目详情。测试库和生产库该配置均已自动初始化但目前为空，需填入可从飞书访问的系统公网地址后再发送一张新卡片验证。Java 测试 60/60 与全部前端 JS 语法检查通过，生产 `c6f3be3` 已完成容器、接口和资源版本验证。
- 生产核验：飞书通道开关为开启；后端已有 3 条 `delivered` 飞书投递。另有 3 条 `failed` 均为卡片 JSON 修复前的历史 `230099/200621` 格式错误，后续投递无同类失败。站内通知目前只完成后端入库、投递与审计，尚无用户侧铃铛/列表界面。
- 已在本地实现“飞书通知模板”独立系统配置组（未提交/未部署）：覆盖项目指派、任务派发/改派/接单/交付/驳回/再次交付、审核、项目/任务催办、临期、逾期与系统告警共 15 个场景，每个场景可编辑标题和正文；支持 `{{projectName}}`、`{{taskName}}`、`{{actorName}}`、`{{deadline}}` 等变量。保存后下一条通知实时读取配置，无需重启。测试库已自动初始化 30 项；完整测试 60/60、模板覆盖测试 3/3、前端语法检查和本地健康检查通过。
- 已进一步降低模板配置门槛（未提交/未部署）：系统管理新增“通知文案”独立页，按 15 个中文业务场景逐卡展示发送时机、标题和正文；隐藏原始配置键。项目名称、任务名称、操作人、截止时间等变量提供中文一键插入按钮，用户无需记忆或手写占位符。原“系统配置”页面不再显示模板技术配置项。完整测试 61/61、前端语法检查通过，本地 `v139` 已重启运行。
- “通知文案”编辑器已随 `097c473` 发布生产：30 项模板配置（15 场景 × 标题/正文）已自动初始化，生产页面加载 `v139`，容器、公开接口与应用日志验证通过。管理员修改并保存文案后，下一条对应飞书通知会实时使用新文本，无需重启。

### IP 二级选项（已发布生产）

- 一级 IP 配置新增二级选项 JSON 与单选/多选规则；管理员可在“IP配置管理”按行或逗号维护二级项。
- 新建渠道定制单和公司常规品时：未选择一级 IP 则二级区域完全隐藏；选择有二级配置的 IP 才展示与目标市场一致的标签式选择区；切换一级 IP 会清空旧选择。
- 项目新增 `ip_sub_options` 保存实际二级选择，项目详情展示“一级 IP / 二级项”；后端严格校验二级项属于所选一级 IP，并执行单选限制。
- 生产迁移 [`2026-07-16-add-ip-secondary-options.sql`](../db/migrations/2026-07-16-add-ip-secondary-options.sql) 已执行并验证三列存在；Java 测试 60/60、全部前端 JS 语法检查、本地 HTTP 与生产公开接口检查通过。生产前端已加载 `v137`。浏览器自动验证未完成，原因是本地浏览器没有登录态；不得代替用户输入账号。

### 本地运行与发布约定

- 用户要求本地项目持续运行。每次修改后必须先重新构建并覆盖本地 `dev` 实例、完成本地验证；当前本地 PID `18705`、`8080` 首页 HTTP 200。
- 未经用户明确提醒，不提交、推送或部署生产；生产发布只能在本地覆盖运行和验证无误后进行。

### 生产状态

- 应用发布提交为 `b090c68774adce61e1665f3430ad2f39895cfac3`，生产已整体切换至用户提供的同一 Base 内 8 张目标表，并支持两级审核同步；本次列表筛选功能已在其上发布。
- 生产已启用主表严格镜像、备份删除标记和系统来源保护；首次全量来源标记已完成，后续每小时空闲对账会按新策略清理符合条件的主表孤儿记录。
- `emie-app` 与 `emie-preview-converter` 正常运行，重启次数为 0；首页、公开配置和预览健康接口均为 HTTP 200。
- 项目、子任务、评分和操作日志均按主表、备份表双写；子任务主备表已分别显示一审/二审状态和得分，评分主备表已生成两条审核记录。

### 当前阻塞

- 当前两级审核与飞书八表同步无功能阻塞。
- 队列为 `done=471`、`fail=8`，无 `pending/processing`；8 条失败仍是目标 Base 切换前保留的历史审计记录，本轮没有新增失败。

### 安全状态与证据

- 已留存生产数据库备份、飞书目标表结构快照、旧配置备份、同步队列备份和两份应用回滚包；具体路径见 [`release-records.md`](release-records.md)。
- 应用容器持续运行，无应用级 `ERROR`；本次未修改项目、子任务、评分或操作日志业务数据。
- 最终飞书 API 核验 8 张表的记录数、业务 ID 集合及子任务项目关联全部通过。
- 详细操作证据见 [`release-records.md`](release-records.md)；配置入口与约束见 [`deployment.md`](deployment.md)。文档中不得写入实际 App Token、Table ID、密钥或数据库凭据。

### 下一位 AI 的处理顺序

1. 后续如验证删除策略，先在测试数据上删除一条业务记录，观察主表删除、备份表保留和删除标记，再进行生产级操作。
2. 每小时对账会在队列空闲时运行；若出现新失败，先检查飞书字段类型、应用权限和同步队列错误信息。
3. 8 条修复前遗留的 `fail` 仅作历史审计保留；如需清理，应作为独立维护操作处理。

#### Java 21 升级

1. 先确认 `docker context ls` / `docker info` 恢复可用。
2. 使用 JDK 21 执行 `./mvnw clean package`，再执行 `docker build --no-cache -t emie-app:java21 .`，确认镜像内 `java -version` 为 21。
3. 容器验证通过后，只提交 Java 21 基线文件和相关文档，不提交 4 个无关模板删除；推送后再按发布手册备份、部署和生产验证，生产当前仍为 `b090c68` / Java 17。

## 3. 已完成的开发内容

### 权限、安全与配置

- 飞书新成员首次登录会自动创建 `pending` 账号，仅显示待授权页；所有业务 API 在统一认证过滤器层返回 403。
- 管理员为待授权账号分配正式角色后，账号自动转为 `active` 并清除旧会话；旧的自助注册接口、注册页面、前端注册逻辑及图片/邮件验证码端点已删除。
- 修复项目及附件跨角色、跨销售可见范围问题。
- 部门负责人可查看本部门成员项目、附件和忙碌状态，但不会自动获得项目修改权限。
- 本地开发使用专用测试 MySQL，连接信息仅通过被 Git 忽略的 `.env` 注入。
- 补充前端输出转义、管理员字段校验、上传类型与大小限制。
- 飞书登录配置在测试库中已启用，配置接口验证通过。

### 业务与文件

- 附件区支持 PDF 和 PPT/PPTX 在线预览。
- 项目新增 IP 选项与管理后台配置。
- 新增 IP 配置时，排序号默认为当前最大排序号加一。
- 飞书项目、子任务和评分同步支持可选备份表双写，并新增队列空闲时的每小时全量对账。
- 已部署主表/备份差异化策略：主表严格镜像数据库并清理系统孤儿记录，备份表永久保留并记录源数据删除状态和时间；人工飞书行不参与清理。
- 本地已实现两级审核记录生命周期：渠道定制为企划→销售，常规品为企划→管理员；二审在一审通过后才进入待审核。
- 子任务飞书同步新增一审/二审角色、状态、审核人和得分，以及两级加权“审核得分”；评分记录与项目表同步审核上下文和进度。

### 前端模块化

- 原单体 `app.js` 已删除，按核心、工作台、项目、管理、文件域拆分。
- 页面只加载 `src/main/resources/static/js/bootstrap.js` 这一个 ES Module 入口。
- `bootstrap.js` 按依赖顺序导入并校验 30 个可注册模块，本地缓存版本已更新为 `v131`。
- 所有业务 JavaScript 文件均已职责化；当前最大文件为 `project-tasks.js`（755 行）。
- 297 个 HTML 内联 `onclick/onchange/oninput/onsubmit` 属性已清零，改用 `data-emie-on*` 声明式事件。
- 业务函数不再直接挂载到 `window`，跨模块调用通过 `EMIE.actions` 解析。
- 散落的 `window._xxx` 业务状态已迁移到 `EMIE.state`、`dashboardState`、`projectState`、`adminState` 和 `fileState`。
- `event-runtime.js` 使用受限动作作用域，会拦截未授权的动态事件表达式。

## 4. 最近验证结果

- 本轮飞书镜像策略回归：`git diff --check` 通过，`./mvnw clean package` 成功，共 54 项测试通过；覆盖系统来源标记、备份删除状态、人工行保护、先清理后入队及清理失败停止写入。
- 生产部署提交 `b090c68` 与远端、`release-sha.txt` 一致；新 JAR SHA-256 为 `62bafd1e` 开头，数据库有效备份与应用回滚包已留存，应用与预览容器重启次数均为 0，首页、公开配置、预览接口 HTTP 200，未登录项目接口 401。
- 生产首次来源标记：8 张飞书表字段类型校验通过；项目主备各 `2/2`、子任务主备各 `2/2`、评分主备各 `2/2`、日志主备各 `155/155`，系统来源标记全部完成；部署后无新增 `FieldNameNotFound`、`ERROR` 或同步失败。
- 生产两级审核发布：数据库备份、旧 JAR 回滚包和同步队列备份均有效；迁移后 5 个审核字段完整，已交付子任务补齐 2 条审核记录且无重复。
- 生产飞书核验：项目主备各 2、子任务主备各 2、评分主备各 2、日志主备各 155；两级审核角色、阶段和状态内容正确。
- 生产运行：提交 `4c70d17` 与远端一致，JAR 哈希一致；应用与预览容器重启次数 0，内外网入口和预览接口 HTTP 200，无新增 `ERROR` 或同步失败。
- 本轮两级审核回归：`./mvnw clean package` 成功，共 48 项测试通过，覆盖两类项目审核角色、审核通过/驳回、二审激活、子任务双评分与加权汇总；本地服务自动重载后 HTTP 200。
- `git diff --check`：通过。
- 飞书字段兼容回归：关联字段使用记录 ID 结构、文本/单选字段保留业务 ID、自动时间跳过写入、日期统一毫秒时间戳；`./mvnw clean package` 共 41 项测试通过。
- 生产飞书八表全量核验：项目主备各 `2/2`、子任务主备各 `2/2`、评分主备各 `0/0`、操作日志主备各 `155/155`；关联关系通过。
- 全部 JavaScript ES Module 语法检查：通过。
- ES Module 运行时合约：30 个模块完整注册，抽查全局业务函数数量为 0。
- 声明式事件分发与危险表达式拦截：通过。
- `./mvnw clean package`：成功，36 个测试全部通过。
- Java 21 兼容性预检：使用本机 Temurin 21.0.11 和 `-Djava.version=21` 编译 78 个源码文件、9 个测试文件，24 个测试全部通过；临时以 `dev` profile 启动在 `18081` 端口，公共配置接口返回 200，随后已停止临时进程。
- Java 21 预检后已重新使用默认 Java 17 执行完整构建，24 个测试全部通过，`target` 构建产物已恢复为 Java 17。
- HTTP 冒烟：首页、`v131` 启动入口及本次修改的组织架构模块均返回 200；此前 30 个导入模块全量检查通过。
- 本轮五个列表页筛选改动：项目列表支持类目、市场、状态、关键词和日期；我的子任务支持归属、状态、类型、关键词和日期；待评分支持评分状态、类型、审核阶段、关键词和日期；修复项目类型切换覆盖全量缓存的问题。`v131` 资源版本已更新，前端语法检查和 `./mvnw clean package` 均通过。
- Java 21 升级实施：`pom.xml`、`Dockerfile`、`README.md`、`RTK.md`、开发指南、部署手册和发布记录已切换到 Java 21 目标基线；业务代码、数据库和 Docker Compose 业务配置未改。JDK 21 编译、55 项测试、class 版本 65 和本地 `dev` 启动冒烟已通过；OrbStack daemon 已恢复，但 Docker 基础镜像拉取因镜像源 EOF 失败，尚未进入项目编译阶段。当前生产仍保持 Java 17。
- 鉴权冒烟：未登录访问项目接口返回 401。
- 注册链路回归：反射测试确认 `AuthController` 不存在 `/register` 处理器，静态资源中不存在注册页面或注册请求逻辑；未认证请求该旧地址由统一鉴权层返回 401。
- 飞书配置冒烟：启用状态和应用 ID 存在。
- Headless Chrome 实际打开本地页面：成功渲染飞书登录界面。
- 生产发布：数据库与旧 JAR 备份有效，新旧产物哈希已核对，两个容器持续运行且重启次数为 0。
- 生产 HTTP：外部入口、首页、公开配置、飞书配置、预览健康检查和 30 个前端模块均通过。
- 生产飞书同步：目标 Base 八表全量重刷 159 条任务全部完成，无新增重试或失败；8 张表记录集合及主表关联字段均已通过飞书 API 核验。

## 5. 工作区与交接风险

- 当前生产已运行 `b090c68`；工作区另有 Java 21 基线文档更新和 4 个无关模板删除，提交前需分开核对。
- 工作区另有 4 个 `docs/templates/project-import/*.xlsx` 被删除，未纳入本轮飞书修改且来源尚未确认；不要随本轮提交，也不要擅自恢复或覆盖。
- 新策略依赖飞书新增字段。生产已完成先补字段、再全量写来源标记；清理只处理“同步来源=系统”且备份存在的记录。
- 两级审核迁移和飞书字段补齐已在生产完成；新增字段为兼容性扩展，旧数据库与应用回滚包均已保留。
- 飞书目标表结构和生产配置包含敏感标识，仅保存在生产备份中，不得复制到仓库、终端回传或对话。
- 大量历史本地改动已于应用提交 `7943e6b` 统一评审、测试并发布；后续以该提交为新基线。
- 飞书全量对账最初与 30 秒队列消费绑定，发布审查时已拆成独立的每小时调度，并增加队列非空跳过保护和回归测试。
- `.env` 含本地测试环境配置且被 Git 忽略，不得输出其内容、不得提交。
- 桌面内置浏览器控制运行时曾因 `Cannot redefine property: process` 无法初始化；已用 ES Module VM 合约测试、HTTP 冒烟和 Headless Chrome 截图验证替代。
- 尚未完成登录后的真实浏览器端到端业务流程验收。
- 备份表只读预检和全量重刷的 422 防护已部署；修改飞书映射前仍需先预检并留存回滚配置。
- 本地 `dev` 实例启动后会立即消费共享测试库中的待处理 `sync_queue` 项，并会刷新飞书 token；本地诊断或测试前应先确认队列为空或显式禁用同步，避免意外发起飞书写入。
- `dev` profile 现默认关闭同步消费者；只有本机显式设置 `APP_FEISHU_SYNC_WORKER_ENABLED=true` 才允许本地队列调用飞书。生产默认保持启用。
- Java 版本存在历史分支混淆：远端 `project_manager_systemv2` 曾在旧根目录使用 Java 21；当前 `project_manager_system` 的升级改动已将 Maven、Docker 和文档统一到 Java 21 目标基线，但生产尚未切换，仍为 Java 17。

## 6. 未完成与建议顺序

1. 拆分 `ProjectService.java`。
2. 拆分 `AdminService.java`。
3. 增加登录、项目创建、任务交付、管理配置和文件预览的真实浏览器端到端测试。
4. 拆分 `static/css/app.css`。
5. 将声明式事件从动作表达式进一步收敛为纯 `data-action + 类型化参数`。

### 新增系统优化清单（2026-07-17）

按优先级记录后续优化范围：

1. 项目查询：所有项目列表、关键字和条件筛选统一下推到分页接口，彻底移除前端全量回退；继续核对慢 SQL、权限范围查询和组合索引。
2. 飞书同步：避免项目、子任务、评分和日志使用 `findAll()` 一次性载入内存；改为分页/增量读取、批量处理，并增加同步任务互斥、超时、失败重试和运行状态监控。
3. 数据库：检查项目、子任务、评分、通知和同步队列表的过滤、关联、更新时间索引，并用 `EXPLAIN ANALYZE` 验证。
4. 通知中心：补齐失败重试、死信、管理员重发、投递耗时/成功率统计和 Open ID 缺失诊断。
5. 前端体验：筛选输入防抖、请求取消、图片缩略图/懒加载、详情页图片稳定渲染和列表缓存。
6. 稳定性：统一异常响应、批量导入重复/日期/配置失效提示、后台任务超时与 JVM/数据库监控。

本轮先执行第 1、2 项，优先降低查询延迟和 Java 内存峰值；完成本地验证后再决定是否进入生产发布。

## 7. 下一位开发者接手步骤

1. 先阅读根目录 `RTK.md` 和本文档。
2. 执行 `git status --short --branch`，保留所有现有改动。
3. 核对当前任务涉及文件，不覆盖其他未提交工作。
4. 验证本地服务：`curl -I http://127.0.0.1:8080/`。
5. 前端改动后更新当前静态资源版本参数，执行 ES Module 语法检查和完整构建。
6. 当前任务结束前，更新本文档的时间、进度、验证、运行状态、工作区风险和下一步。

## 8. 最近进度记录

- `2026-07-17`：已将当前用户 `~/.zshrc` 的默认 `JAVA_HOME` 从 WorkBuddy Java 17 切换为本机 Java 21（`jdk-21.0.11+10`）；新 zsh 已验证 `java -version` 和 Maven 均使用 Java 21。仅修改本机 shell 环境，未提交、推送或部署。
- `2026-07-17`：用户确认继续优化。用户管理新增 `/api/admin/users/page` 分页接口，支持关键词、角色、状态在数据库侧筛选，管理页面改为每页 30 条、分页切换和筛选均请求后端；组织架构使用的旧全量 `/api/users` 保持不变。仪表盘项目摘要改为后端分页只返回最近 15 条；项目总数、渠道定制数、公司常规品数和进行中项目数改为数据库聚合统计，完整项目通过项目列表分页页查看。Java 21 构建 65/65、全量 JavaScript 语法检查、`git diff --check` 通过，本地服务 PID `36782`、HTTP 200。当前改动仅在本地，尚未提交、推送或部署生产。

- `2026-07-17 00:16 +0800`：左侧导航统计修复提交 `62502d7` 已推送并发布生产。发布前有效数据库备份为 `/root/emie/db-backup-before-62502d7-20260717_001426.sql.gz`（38077 字节，`gzip -t` 通过），无数据库迁移；旧 JAR 回滚包已保留。生产 JAR 哈希与本地一致，应用/预览容器正常，公开配置与预览健康 HTTP 200，未登录徽章接口 401，首页已加载 `v146`，本次启动日志无应用错误。实际登录后的页面切换和业务操作后徽章稳定性待用户验收。
- `2026-07-17 00:12 +0800`：完成左侧导航徽章统计本地修复。根因是三套前端写入逻辑相互覆盖，项目操作路径读取不完整响应后把总项目、渠道和常规品徽章写为 0。新增唯一后端统计口径和唯一前端写入函数，统计身份由会话确定，项目数量与对应导航列表一致，待评分与评分中心一致；历史 N+1 工作台统计入口已转发统一函数。Java 21 `clean package` 65/65、全量 JS 语法检查和差异检查通过；本地 Java PID `84883`，公开配置 200，未登录徽章接口 401。当前改动未提交、未推送、未部署。
- `2026-07-17 00:04 +0800`：项目服务端筛选与 CRUD 规范第一阶段提交 `bc396bf` 已推送并发布生产。发布前有效数据库备份为 `/root/emie/db-backup-before-bc396bf-20260717_000222.sql.gz`（37510 字节，`gzip -t` 通过），三条项目筛选索引迁移已执行并核验；旧 JAR 回滚包已保留。生产 `release-sha.txt` 与 Gitee 一致，JAR SHA-256 与本地一致，应用/预览容器正常，公开配置与预览健康 HTTP 200，未登录分页接口 401，首页为 `v145`，本次启动日志无应用错误。浏览器登录后的筛选交互待用户验收。
- `2026-07-16 23:42 +0800`：按第一性原理完成项目查询与 CRUD 规范第一阶段本地实现。筛选从浏览器全量数组下推为分页接口参数，新增项目动态查询仓储、统一分页/参数错误 DTO、三条项目筛选组合索引迁移和 CRUD 接口规范文档。动态查询只添加实际条件，避免 `:param IS NULL OR`；权限范围继续由服务端会话/部门负责人范围确定，旧全量接口仅兼容旧页面。Java 21 `clean package` 64/64、全量 JS 语法检查、差异检查通过；本地 Java PID `77576`、公开配置 200、未登录分页接口 401。当前改动未提交、未推送、未部署。
- `2026-07-16 23:31 +0800`：完成项目查询接口只读诊断，未修改业务代码或生产配置。默认项目列表已走 `/api/projects/page`（每页 15 条），单次请求通常包含分页数据、总数、子任务聚合计数和评分聚合四类数据库查询；现有批量聚合已避免评分/子任务 N+1。当前最大剩余瓶颈是前端在首次使用关键字或条件筛选时回退调用旧的全量 `/api/projects`，随后在浏览器过滤，数据增长后会抵消分页收益。下一步应将关键词、状态、类目、市场、日期等筛选下推到分页接口；同时拆分仓储层 `:type IS NULL OR ...` 条件，并重点优化设计师/供应链 `DISTINCT + JOIN + COUNT(DISTINCT)` 与企划可见范围中的 `OR` 查询。当前测试库项目仅 28 条，不足以复现大数据量慢查询，实施前应采集生产慢 SQL / `EXPLAIN ANALYZE`。
- `2026-07-16 10:05 +0800`：生产服务器独立 Dockerfile 已切换为 `eclipse-temurin:21-jre`，重新构建并部署提交 `af7dd39`。生产容器重启次数 0，应用日志已确认 Java 21，数据库连接成功，首页和公开配置接口 HTTP 200，生产 JAR SHA 与本地一致。
- `2026-07-16 10:09 +0800`：负载控制提交 `9ea41e3` 已推送并部署生产。生产默认 Hikari 最大连接 10、最小空闲 2，Tomcat 最大线程 100、最大连接 200、排队 50；虚拟线程保持可配置开启。`./mvnw clean package` 55 项测试通过；生产容器重启次数 0，首页和公开配置接口 HTTP 200，预览容器正常。
- `2026-07-16 10:15 +0800`：只读检查生产内存：主机 1.6GiB，总已用约 1.3GiB、可用约 332MiB；`emie-app` 约 342MiB、MySQL 约 348MiB、预览转换器约 97MiB。应用 JVM 参数为 `Xmx256m/Xms128m/MaxMetaspace128m`，进程 RSS 约 366MiB；未修改线上参数。后续可先将应用 `Xmx` 调至 192m、`Xms` 调至 64m、MaxMetaspace 调至 96m，观察 GC 和接口延迟，再评估 MySQL 缓冲池。
- `2026-07-16 10:19 +0800`：使用 Java 21、`Xmx192m/Xms64m/MaxMetaspace96m/SerialGC` 在本地 `dev` profile 的 18082 端口完成真实启动验证，Tomcat、JPA 与 Hikari 数据库连接均成功，随后已正常停止临时进程。该参数可作为生产第一阶段内存收紧候选，仍需生产发布后观察 GC、接口延迟和容器重启。
- `2026-07-16 10:30 +0800`：发现将生产 Metaspace 收紧至 96MiB 后出现 `OutOfMemoryError: Metaspace`，造成项目查看等请求 500；已立即恢复为 `Xmx192m/Xms64m/MaxMetaspace128m/SerialGC` 并重启。应用容器重启次数 0，首页和公开配置 HTTP 200。全部项目、渠道/常规品、我的子任务、待评分等“查看项目”入口均共用 `GET /api/projects/{id}`，本次为全局运行时内存问题而非单页逻辑问题。
- `2026-07-16 10:35 +0800`：诊断项目详情滚动后缩略图闪烁：详情图片同时使用 `loading=lazy` 与 `.image-preview img` 的 `content-visibility:auto`。在滚动弹窗中离开/回到可视区域会触发跳过与恢复绘制，属于前端渲染策略，不是服务器或 Java 内存问题。后续可移除该规则中的 `content-visibility:auto`，保留固定缩略图尺寸和 lazy loading。
- `2026-07-16 11:06 +0800`：项目详情图片闪烁修复提交 `d48b7b8` 已推送并部署生产：移除 `.image-preview img` 的 `content-visibility:auto`，保留固定尺寸和 lazy loading；CSS 缓存版本升至 `v19`。生产容器重启次数 0，首页和公开配置接口 HTTP 200，生产确认加载 `app.css?v=19`。
- `2026-07-16 11:12 +0800`：用户确认启动完整通知中心需求。已新增 `docs/notification-system-design.md`，固化全场景必达、任务独立状态版本、再次交付、飞书卡片、站内通知、催办、独立审计、重试/死信及分期实施方案；当前进入第一步通知基础设施实现，未提交、推送或部署。
- `2026-07-16 11:26 +0800`：通知中心第一步基础实施完成：新增 `notification_events`（事务 Outbox）、`notifications`（站内收件箱）、`notification_deliveries`（渠道投递）和 `notification_audit_logs`（追加式审计）的实体、仓库和可重复执行迁移；新增 `NotificationOutboxService`。`NotificationOutboxServiceTest` 覆盖新事件创建审计及同一幂等键技术重试不重复创建。`./mvnw clean package` 55 项测试通过，新增目标测试 2 项通过；尚未提交、推送或部署。
- `2026-07-16 11:29 +0800`：用户明确本地优先发布约定：本地服务需持续运行，每次改动后先覆盖本地 `dev` 运行实例并验证，再等待用户指令推送生产。当前 Java PID `66795`，首页 HTTP 200。
- `2026-07-16 11:35 +0800`：通知中心配置项已接入现有“系统配置”页：新增全局/站内/飞书开关、外送重试上限、人工催办间隔、跨用户展示合并窗口、逾期升级时长和普通动态摘要开关；关键必达通知规则仍由后续编排器强制执行。前端资源版本升至 `v132`，完整构建 57 项测试通过、全部 JS 模块语法检查通过；本地 Java PID `18705` 已覆盖启动，首页 HTTP 200。
- `2026-07-16 11:41 +0800`：通知中心第一阶段提交 `2419054` 已推送并发布生产。发布前数据库备份 `db-backup-before-notification-center-20260716_113830.sql.gz` 已通过 `gzip -t`；新增四张通知表迁移成功，生产自动初始化 8 项 `notification` 系统配置。生产 JAR SHA 与本地一致，应用/预览容器重启次数 0，首页和公开配置接口 HTTP 200；未触发任何实际业务通知或飞书外送。
- `2026-07-15 22:28 +0800`：五个业务列表页筛选功能已提交 `b090c68` 并推送到 `emie/project_manager_system`，远端 SHA 与本地一致。精确提交干净工作树构建成功，55 项测试全部通过；生产已备份数据库和旧 JAR，部署后 `release-sha.txt`、JAR SHA 与目标提交一致。首页、公开配置、预览健康接口均 HTTP 200，未登录项目接口 401，`bootstrap.js?v=131`、项目/子任务/待评分筛选模块均验证生效，两个容器重启次数均为 0。首次缺少 `PROCESS` 权限的备份已标记 `.invalid`，有效备份使用 `--no-tablespaces` 生成。生产发布完成。
- `2026-07-15 20:55 +0800`：飞书主表严格镜像与备份永久保留已发布生产。提交 `5a2a49b` 与远端和生产一致；数据库备份、同步队列备份和应用回滚包有效。生产先补齐 8 张表的来源/备份状态/删除时间字段，再完成 161 条当前业务记录首次来源标记；最终队列 `done=471`、`fail=8`、无 `pending/processing`，8 条失败为历史审计记录。8 张表 API 核验通过，应用与预览容器重启次数 0，应用和预览接口 HTTP 200，部署后无新增字段或应用错误。
- `2026-07-15 20:17 +0800`：按确认完成飞书差异化同步策略的本地实现。四张主表按数据库业务 ID 严格镜像，四张备份表永不删除并在源数据删除时写入状态和删除时间；仅处理“同步来源=系统”的记录，主表删除前必须存在系统备份。每小时对账和管理员全量同步均先清理后入队，飞书读取或清理失败则整轮停止。`git diff --check` 与 `./mvnw clean package` 通过，54 项测试成功；本地 PID `99681`、HTTP 200、同步消费者关闭。尚未提交、推送、修改生产飞书字段或部署。
- `2026-07-15 18:27 +0800`：两级审核同步已发布生产。应用提交 `4c70d17` 与远端、生产一致；数据库和旧 JAR 备份有效，迁移补齐 5 个评分审核字段及 2 条历史审核记录，主备项目/子任务/评分表审核字段均已补齐。161 条全量同步全部完成，无新增失败；飞书 API 核验项目主备各 2、子任务主备各 2、评分主备各 2、日志主备各 155，企划一审“待审核”和管理员二审“等待一审”正确。两个容器重启次数 0，内外网及预览接口 HTTP 200，日志无新增错误。
- `2026-07-15 17:52 +0800`：针对子任务表只有一个审核得分的问题，完成两级审核本地实现。渠道定制建立企划一审、销售二审，常规品建立企划一审、管理员二审；子任务飞书主备表新增一审/二审角色、状态、审核人、得分和加权审核得分，评分记录与项目表同步审核上下文及进度。新增数据库迁移、审核字段补齐接口和 7 项回归测试，`git diff --check` 通过，`./mvnw clean package` 48 项测试全部通过；本地服务自动重载后 HTTP 200。本轮尚未提交、推送、修改飞书表或部署生产。
- `2026-07-15 16:58 +0800`：完成飞书八表生产切换与全量验证。生产最终运行 `d888b9b`，应用与预览容器正常、重启次数 0，关键接口 HTTP 200。本轮 159 条全量任务全部完成，无新增重试或失败；飞书 API 核验项目主备各 2、子任务主备各 2、评分主备各 0、操作日志主备各 155，业务 ID 与数据库一致，子任务项目关联正确。数据库、飞书结构、旧配置、同步队列和应用包均已备份。
- `2026-07-15 16:34 +0800`：首次生产验证发现双向关联字段需要直接传记录 ID 字符串数组，2 条子任务首次调用返回参数错误。立即停止应用避免继续重试，修正代码和回归测试，41 项测试通过后提交并发布 `d888b9b`；保留原队列后成功重试，无数据丢失。
- `2026-07-15 16:13 +0800`：完成飞书八表同步本地兼容实现。同步时动态读取字段类型：子任务“所属项目”和评分“所属子任务”在关联字段中写入飞书记录 ID，在文本/单选字段中保留业务 ID；日期改为毫秒时间戳，自动创建时间不再覆盖；操作日志改为主表和备份表双写。新增 5 项字段兼容测试，`./mvnw clean package` 共 41 项测试通过；本地 `dev` 服务 PID `99681`、HTTP 200，消费者保持关闭。生产配置和飞书目标表尚未变更。
- `2026-07-15 15:33 +0800`：用户提供其飞书 Base 的实际表清单：「项目总表、子任务表、评分记录表、操作日志表」及四张对应 `_backup` 表，共 8 张。该清单与生产当前 `feishu.base.appToken` 可见的 7 张「数据表、子任务管理、评分管理」命名表不一致，说明当前生产配置绑定的是另一份 Base；这解释了用户在目标 Base 看不到系统同步数据。后续修复需将生产 Base App Token 及主表/备份表映射整体切换到用户提供的这份 Base，并确认企业自建应用有访问权限；操作日志主表双写目前代码未实现，现有逻辑仅写操作日志备份表。
- `2026-07-15 15:29 +0800`：针对用户所述「项目总表 / 项目总表_backup」为空，生产只读核对配置映射与当前 Base 表名。项目同步实际绑定「数据表」，项目备份实际绑定「数据表 backup」；当前 Base 的 7 张表中不存在「项目总表」或「项目总表_backup」。因此系统不会向用户所述名称的表写入，需在飞书中查看上述实际绑定表，或作为后续配置变更任务将项目同步映射改到指定的新表。
- `2026-07-15 15:25 +0800`：用户在同一 Base 仍看不到同步数据时，飞书 API 复核确认主表与备份表均有记录，且原公开视图无筛选、无隐藏列；项目备份 2 条、子任务备份 2 条、操作日志备份 149 条均可在对应视图检索到。为绕开用户个人视图或客户端展示状态，已在三张非空备份表各新增公开网格视图「同步数据无筛选」，并通过该新视图验证记录可见。无业务数据修改。
- `2026-07-15 15:18 +0800`：用户请求立即手动同步时，生产每小时对账已于 `15:07` 自动将当前 153 条记录重新入队；为避免重复写入，未额外重复入队。该轮已全部完成且无新增失败，最终无 `pending/processing`。飞书 API 只读核对当前绑定备份表的实际记录数：项目 2、子任务 2、评分 0、操作日志 149；若飞书页面仍为空，应检查是否打开了同一个 Base 及正确视图。
- `2026-07-15 14:57 +0800`：按用户确认将飞书备份表同步推送到生产。操作前备份 `sync_queue` 至 `/root/emie/sync-queue-before-full-resync-20260715_144310.sql.gz`，`gzip -t` 通过；按全量重刷去重规则复用并重置 4 条历史失败任务、入队 149 条操作日志，共处理 153 条。同步工作线程分批完成全部任务，无新增失败；最终队列为 `done=304`、`fail=8`、无 `pending/processing`，8 条为修复前遗留历史。飞书 API 只读复核：项目备份 `2/2`、子任务备份 `2/2`、评分备份 `0/0`、操作日志备份 `149/149`，应用容器持续运行且重启次数为 0。
- `2026-07-15 14:34 +0800`：按用户授权修复生产飞书备份表映射。先生成并验证数据库备份 `/root/emie/db-backup-before-feishu-backup-config-20260715_143402.sql.gz`（`gzip -t` 通过）；此前一次包含 tablespace 的备份因 MySQL `PROCESS` 权限提示已标记为 `.invalid`，不作为回滚依据。复用当前 Base 内已有的「数据表 backup、子任务管理 backup、评分管理 backup」，新建「操作日志 backup」并按同步代码所需字段完成校验（无需补字段），将四项备份表配置改为当前 Base 中对应表。飞书 API 后检：当前 Base 共 7 张表，四项配置均映射正确；应用容器未重启，重启次数仍为 0。此次未触发单条、失败队列重试或全量同步；下一步可由管理员确认后执行一次同步验证，再视结果处理既有失败队列。
- `2026-07-15 14:26 +0800`：用户更新四张飞书备份表配置后，执行了真实的只读备份表预检：获取企业自建应用 token 并列出当前 App Token 对应 Base 的表，未创建、更新或重试任何飞书记录。当前 Base 可见 6 张表，但项目、子任务、评分、操作日志四张备份表均不匹配，预检失败。四个 Table ID 均为 16 位 `tbl...` 格式且更新时间为 `14:23:09`；需要将备份表移动/创建到当前 Base，或改用该 Base 内的 Table ID 并授予企业自建应用访问权限。修正前不要触发全量重刷。
- `2026-07-15 14:29 +0800`：继续只读列出当前 Base 的可见表，名称为「数据表、子任务管理、评分管理、数据表 backup、子任务管理 backup、评分管理 backup」。前三张备份表已在当前 Base 内存在，但当前配置的 ID 不匹配；应从这三张 backup 表各自 URL 的 `table=` 参数重新复制 Table ID。当前 Base 不存在操作日志 backup 表，需先在同一 Base 创建该表后再填写其 ID。
- `2026-07-15 14:12 +0800`：对飞书多维表格同步做了生产只读核对，未触发同步、未修改配置或业务数据。主表同步配置已启用且主表 ID 格式正常；项目、子任务、评分三张备份表 ID 格式正常但仍因 `TableIdNotFound` 无法访问。队列 `done=151`、`fail=12`，失败项为项目 6 条、子任务 6 条，均已重试 3 次；自动对账会再次触发失败重试。操作日志备份表未配置。下一步应在系统管理中执行备份表预检，确认三张表均位于当前 Base 且企业自建应用已获授权，再进行单条验证和全量重刷。
- `2026-07-15 14:03 +0800`：生产已部署 `38bdfc3`。数据库备份 `/root/emie/db-backup-before-38bdfc3-20260715_135938.sql.gz` 为 15134 字节且 `gzip -t` 通过，回滚 JAR 已保存。两个容器正常、应用重启次数 0，首页、公共配置和预览健康接口 HTTP 200；生产已加载 `v130` 和组织架构动态角色逻辑，无新增应用错误。
- `2026-07-15 13:52 +0800`：修复组织架构遗漏自定义角色的问题。`/api/users` 从固定 5 角色分组改为保留标准空分组并动态加入所有非待分配角色，`Promotion` 用户可进入“未分配部门的用户”；组织架构角色名称、新建部门和编辑部门关联角色统一读取角色管理数据，并兼容大写角色键徽章。缓存版本更新至 `v130`，JavaScript 语法检查与 `./mvnw clean package` 36 项测试通过，本地 PID `65226`、首页和组织架构模块 HTTP 200。尚未提交、推送或部署。
- `2026-07-15 12:38 +0800`：生产已部署 `739b17d`。数据库备份 `/root/emie/db-backup-before-739b17d-20260715_123622.sql.gz` 为 15131 字节且 `gzip -t` 通过，回滚 JAR 已保存。两个容器正常、应用重启次数 0，首页、公共配置和预览健康接口 HTTP 200；生产已加载 `v129`、角色类名小写规范化逻辑与产品推广 CSS，无新增应用错误。
- `2026-07-15 12:33 +0800`：定位产品推广徽章仍为黑字透明背景的根因：生产角色键为 `Promotion`，生成的 `role-Promotion` 无法命中小写 `.role-promotion` 样式。用户列表、编辑用户和修改角色弹窗现统一将徽章 CSS 类名规范为小写，新增大小写回归测试，缓存版本更新至 `v129`；JavaScript 语法检查与 `./mvnw clean package` 34 项测试通过，本地 PID `53537`、首页和角色模块 HTTP 200。待提交、推送与生产发布。
- `2026-07-15 12:24 +0800`：生产已部署 `5d9f6c7`。数据库备份 `/root/emie/db-backup-before-5d9f6c7-20260715_122337.sql.gz` 为 15343 字节且 `gzip -t` 通过；两个容器正常、应用重启次数 0、健康接口 HTTP 200、日志无应用错误。生产首页已引用 `app.css?v=18`，产品推广徽章的紫色渐变背景、边框和阴影规则已验证生效。
- `2026-07-15 12:23 +0800`：确认生产已包含 `promotion` 紫色规则，但页面仍引用 `app.css?v=17`，用户浏览器命中旧 CSS 后显示为黑字透明背景。本地将样式升级为紫色渐变背景、边框和阴影，并把 CSS 缓存版本提升至 `v18`；`./mvnw clean package` 33 项测试通过，本地 PID `48619`、HTTP 200。尚未提交、推送或部署。
- `2026-07-15 12:18 +0800`：生产已部署 `2411964`。数据库有效补备份为 `/root/emie/db-backup-after-2411964-20260715_121741.sql.gz`（15126 字节，`gzip -t` 通过）；主机缺少 `mysqldump` 导致首次空压缩文件已标记为 `.invalid`，随后改用 MySQL 容器完成备份。本次无数据库结构变更。两个容器正常、重启次数 0、公共配置接口 HTTP 200、日志无应用错误，生产首页为 `v128`，动态角色中文映射和 `promotion` 徽章资源均已验证。
- `2026-07-15 12:11 +0800`：修复自定义角色展示仍回退英文标识的问题。用户管理列表、筛选和当前角色徽章改为读取角色表显示名，为 `promotion` 增加紫色头像与徽章样式，缓存版本更新至 `v128`；同时修正 `dev` profile 数据源 YAML 层级，恢复专用测试 MySQL。JavaScript 语法检查及 `./mvnw clean package` 33 项测试通过，本地 PID `44232`、HTTP 200。尚未提交、推送或部署。
- `2026-07-15 11:56 +0800`：生产已部署 `1b45f73`。两个容器正常运行，公共配置接口 HTTP 200，生产首页确认加载 `bootstrap.js?v=127`；自定义角色下拉修复已进入生产。
- `2026-07-15 11:48 +0800`：新增第四张飞书操作日志备份表配置、预检、全量入队与周期性对账同步；用户管理角色下拉改为动态读取角色表，后端改为校验角色存在性。静态资源版本更新至 `v127`，`./mvnw clean package` 33 项测试通过。待提交、推送与生产发布。
- `2026-07-15 09:55 +0800`：完成飞书备份表同步防护本地修复：新增只读备份表预检端点，并将全量重刷改为预检失败即返回 422、不入队；`dev` profile 默认禁用同步消费者，防止共享测试库待处理任务在本地启动时写入飞书。新增 2 个控制器回归测试，`./mvnw test` 共 33 项通过；本地服务以 PID `11979` 重启，`8080` HTTP 200。未修改生产配置、未重试生产失败任务、未提交或推送。
- `2026-07-15 09:33 +0800`：按用户要求恢复本地 `dev` 服务，PID `6493`，`8080` 公共配置健康检查通过。启动日志发现共享测试库有 1 条待处理同步任务，消费者已启动并刷新飞书 token；本轮未触发全量同步、未修改飞书配置或业务数据。已完成备份表故障只读诊断：生产阻塞仍是备份 Table ID 与当前 Base/应用权限不匹配；现有代码缺少配置保存前的只读表可访问性预检，后续先设计预检与本地同步隔离，再进行单条验证。
- `2026-07-14 18:28 +0800`：按用户要求触发备份表立即全量同步；操作前已备份 `sync_queue`。生产共有 2 个项目、2 个子任务、0 条评分记录，共入队 4 条任务；12 次调用全部返回 `TableIdNotFound`，最终 4 条任务转为 `fail`。应用运行正常，未修改业务数据；需先将备份表放入当前同一个 Base 或提供该 Base 下可访问的 Table ID。
- `2026-07-14 18:11 +0800`：应用提交 `7943e6b` 已推送并部署生产；数据库和旧 JAR 备份有效，容器、外部入口、关键接口、30 个模块、注册移除及首轮飞书全量对账验证通过。备份表双写等待管理员填写 3 个 Table ID。
- `2026-07-14 18:00 +0800`：将飞书备份表双写与周期性全量对账纳入发布；修复对账每 30 秒全量重入队的生产风险，改为默认每小时且仅在队列空闲时运行，新增 2 项调度回归测试，31 项测试全部通过。
- `2026-07-14 17:54 +0800`：彻底删除遗留自助注册链路，包括后端接口、注册页面、前端注册逻辑、图片验证码与邮件验证码端点；缓存版本更新至 `v126`，29 项测试及全量 JavaScript 语法检查通过，本地新包已在 `8080` 重启并完成 30 模块 HTTP 冒烟。
- `2026-07-14 17:42 +0800`：实现飞书新成员待授权流程、后端业务 API 隔离、管理员角色分配后自动启用、旧会话失效和自助注册关闭；全量 JavaScript 语法检查及 29 项测试通过；本地新包已在 `8080` 运行并完成 HTTP 冒烟。
- `2026-07-14 17:30 +0800`：评估从 Java 17 升级 Java 21 的工作量。代码无需先行修改即可完成 Java 21 编译、24 项测试和临时启动冒烟，判断升级风险低、工作量小；本轮未修改 Java 基线，验证后已恢复 Java 17 构建产物，本地 `8080` 服务保持运行。
- `2026-07-14 17:19 +0800`：核对 Java 版本。确认当前业务分支、Maven、Docker 与本地运行进程均为 Java 17；历史 Java 21 改动位于另一条远端分支且并未形成统一的 Java 21 基线。本轮仅诊断和记录，未修改运行配置。
- `2026-07-14 17:03 +0800`：固化“每次进度必须更新交接文档”的协作规则，建立当前完整交接快照。
- `2026-07-14 16:49 +0800`：前端模块化收尾，完成 ES Module、声明式事件、30 模块运行时验证、全量构建和本地重启。
- `2026-07-14 15:26 +0800`：将项目域拆分为上传、创建表单、详情、任务流程和分享子模块。
## 2026-07-17 本地优化进度（通知失败闭环）

- 完成飞书通知失败/死信管理闭环：新增管理员接口 `GET /api/admin/notifications/failures` 查询最近失败与死信记录，新增 `POST /api/admin/notifications/deliveries/{id}/retry` 进行人工重新排队；人工操作写入通知审计日志，自动重试仍按退避策略执行。
- 系统管理“通知中心”增加“飞书失败通知”区域，可查看收件人、通知标题、失败原因、重试次数，并点击“重新发送”。
- 本地验证：`./scripts/mvnw-java21.sh clean package`，65 项测试全部通过；`admin-shell.js` 语法检查通过；`git diff --check` 通过；本地服务已重启并保持运行，`http://127.0.0.1:8080/` 返回 HTTP 200。
- 本轮未提交、未推送仓库、未部署生产；数据库迁移仍需按既有发布流程执行后再启用生产管理页面。
- 继续完成三项基础优化：新增高频查询索引迁移 `2026-07-17-add-query-hotpath-indexes.sql`；新增 API 耗时响应头 `X-Response-Time-Ms`，超过 500ms 的接口记录慢请求日志；前端 `apiGet` 对用户、角色、组织、类目、IP 和系统配置等稳定 GET 请求增加 10 秒短缓存，并在写操作后自动清空，避免重复请求和脏数据。
- 本轮继续优化运行时治理：新增管理员运行指标接口 `GET /api/system/metrics`，提供 JVM 已用/最大内存、CPU 核数及飞书同步队列 pending/processing/fail 数量；用于后续状态面板和告警接入。Java 21 下 65 项测试通过，本地服务重启前保持运行，未提交、未推送、未部署。
- 本轮补齐运行时告警：每 60 秒检查 JVM 内存、飞书同步队列和 Hikari 数据库连接池，超过阈值或队列存在失败任务时输出结构化 WARN 日志；阈值可通过 `monitoring.jvm-used-percent`、`monitoring.sync-queue-pending`、`monitoring.alert-interval-ms` 配置。导入 IP 选项时改用最大排序值查询，去除一次全量 `findAll()`。
- 2026-07-17 回归修复：本地页面 500 的根因是实体已使用 `sub_tasks.updated_at` / `scoring_records.updated_at`，但共享测试库未执行对应迁移，Hibernate 报 `Unknown column`。已补齐本地测试库字段、重启服务并验证首页 200；迁移脚本改为 `ADD COLUMN IF NOT EXISTS`，避免重复执行时报错。后续发布必须先执行该迁移，再启动新版本应用。
- 2026-07-17 工作台性能补充：角色状态接口和项目角色状态接口纳入前端 10 秒缓存，避免工作台、状态面板和任务弹窗重复查询；修正旧 `/dashboard/stats` 接口使用角色权限聚合统计，避免重新加载全量项目后在内存中计算。此前端缓存写操作会自动失效。
- 2026-07-17 工作台核心优化：`/dashboard/full` 不再加载全部可见项目实体用于统计，改为按权限仅查询项目 ID，再用数据库聚合子任务状态和待评分数量；项目实体仍只加载首页 15 条摘要。Java 21 下 65 项测试通过，本地服务已重启并保持运行。
- 2026-07-17 工作台回归修复：MySQL 开启 `ONLY_FULL_GROUP_BY` 时，项目 ID 去重查询同时按未选出的 `created_at` 排序会触发 3065，导致工作台接口整体失败。已移除该统计 ID 查询中不必要的排序，保留首页项目分页查询的排序；65 项测试通过，本地服务已重启。
- 2026-07-17 工作台角色面板修复：产品企划查看设计师/供应链状态时，原权限解析只允许“查看本人同角色”，导致两个面板返回空数据。现允许产品企划读取活跃设计师和供应链状态，仍不改变项目编辑权限；本地服务需重启后生效。
- 2026-07-17 Safari 日期控件修复：新建项目和子任务日期字段原先使用文本框加动态透明 `input[type=date]` 模拟选择器，Safari 对动态隐藏控件的 `showPicker/click` 触发不稳定。现改为直接渲染原生日期输入框，按钮仅触发真实控件，并保留 Safari 回退点击逻辑。
- 2026-07-17 Safari 日期控件第二次修复：Safari 对按钮事件中脚本调用原生日期选择器仍存在限制。日期图标改为关联真实输入框的 `<label for>`，由浏览器原生激活，不再依赖 `showPicker()` 或动态点击；Chrome、Edge、Firefox 仍保持原生日期控件行为。
- 2026-07-17 Safari 日期缓存修复：确认新建渠道定制单和公司常规品页面仍加载旧的 `core-ui.js?v=144`，导致前两次日期控件代码实际未生效。已统一前端模块版本升级至 `v145`，入口升级至 `bootstrap.js?v=147`，并通过 65 项测试、本地重启和 HTTP 200 验证。
- 2026-07-17 日期控件最终简化：原生 `input[type=date]` 已自带日历入口，额外的外置按钮会造成双日历图标且部分浏览器无法触发。现删除外置按钮，仅保留浏览器原生日历控件，统一 Safari、Chrome、Edge、Firefox 行为。
- 2026-07-17 日期输入框体验优化：日期输入框本身增加点击打开原生日历的处理，整个输入区域都可操作；删除外置按钮后的残留布局改为完整圆角、100% 宽度，修复右侧缺块问题。
- 2026-07-17 日期输入框边框修复：为日期控件补充更高优先级的完整边框、`box-sizing` 和四边圆角，并提升 CSS 缓存版本至 `v20`，修复右侧边框缺失/方角问题。
- 2026-07-17 日期控件视觉优化：隐藏 Chrome/WebKit 和 Firefox 原生日历小图标，保留整块输入框点击打开日期选择器，CSS 缓存版本升级至 `v21`。
- 2026-07-17 Firefox 日期图标兼容修复：Firefox 部分版本不响应 `::-moz-calendar-picker-indicator`，新增 Firefox 专用 `-moz-appearance/textfield` 规则隐藏原生小按钮，日期输入和点击选择行为保持不变，CSS 缓存版本升级至 `v22`。
- 2026-07-17 生产发布结果：首次部署 `2f5d0b5` 因 `SyncWorker` 双构造器未显式指定注入构造器导致启动失败，已按流程回滚并恢复 HTTP 200；修复为 `0ece997` 后重新推送、部署成功。生产当前运行 `0ece997`，数据库备份和 4 个迁移已完成，应用/预览健康检查均通过。后续人工验收工作台、通知、日期控件和飞书同步。
- 2026-07-17 生产备份清理：生产 `/root/emie` 原有约 5.6GB、112 个历史备份文件，已按类别清理，仅保留数据库 2 份、应用回滚包 2 份、同步队列 2 份和 Dockerfile 1 份；当前备份占用约 187MB，应用与预览容器未受影响。
- 2026-07-17 生产导入完成：按用户确认的最新模板仅导入「渠道定制单」3 条项目，项目 ID 为 32、33、34；价格区间均为「100元以下」，IP 为「三丽鸥」、二级 IP 为 `HelloKitty`，日期分别为 2026-10-20、2026-08-30、2026-10-30。生产应用 HTTP 200，导入操作日志同步状态 done；公司常规品未处理。
# 2026-07-17 项目扫描结论（仅诊断，未改业务代码）

- 本次扫描确认当前分支工作区干净，未执行重启、提交、推送或生产变更。
- 高优先级待优化：`AdminService#getWorkloadStats` 按用户逐个执行统计 SQL，存在 N+1；通知失败列表逐条查询通知实体，也存在 N+1。
- 高优先级待治理：近期迁移脚本中的 `CREATE INDEX`、`ALTER TABLE ADD COLUMN` 缺少统一幂等机制，当前依赖人工执行顺序；建议引入迁移版本表或 Flyway/Liquibase，并补充启动集成测试。
- 高优先级待治理：运行时告警目前仅输出日志，失败队列告警无去重/冷却，也没有直接接入飞书告警；同步失败需要可观测的指标、告警和处理闭环。
- 中优先级：`ProjectService`、`AdminService`、`FeishuBaseService` 仍偏大，职责可拆分；前端缓存已有短 TTL 和请求去重，但请求取消、页面级缓存边界和资源版本号仍可统一。
- 运维侧需持续关注：生产容器 JVM 上限约 192MB、应用容器 640MB，主机内存余量偏紧，应结合真实峰值监控后再调整连接池、线程和预览转换资源。

## 2026-07-20 子任务页面视觉主对象修正（本地）

- 原页面查询的是子任务，但视觉上仍容易被理解为项目列表；已改为“子任务：名称/#编号”为主信息，所属项目降为次级关联信息，并显示当前用户关系。
- 前端资源版本更新至 `bootstrap.js?v=154`；Java 测试、JS 语法检查、`git diff --check` 通过，本地服务 HTTP 200。
- 当前改动未提交、未推送、未部署生产。

## 2026-07-20 其他子任务页面与工作台跳转（本地）

- 新增“其他子任务”只读页面入口；管理员或销售/企划部门负责人可查看部门成员关联任务，普通成员由后端返回 403，不依赖前端隐藏菜单。
- 其他任务卡片显示“我发布的任务 / 部门成员关联任务”等关系，隐藏接单、交付、评分等写操作按钮。
- 工作台子任务卡片分别跳转到全部、待处理、已完成对应二级页面；顶部卡片数字逻辑暂未修改。
- 新增 `GET /api/projects/department-subtasks`，部门成员范围由后端部门负责人关系计算；资源版本更新至 `bootstrap.js?v=155`。
- Java 测试、JS 语法检查、`git diff --check` 通过，本地服务 HTTP 200；当前未提交、未推送、未部署生产。

## 2026-07-20 工作台卡片布局调整（本地）

- 工作台第一排固定为：我的项目、渠道定制单、公司常规品、进行中项目。
- 第二排固定为：渠道子任务总数、待处理子任务、已完成子任务、待评分。
- 新增渠道子任务统计字段 `channelTasks`；子任务卡片跳转逻辑保持全部/待处理/已完成分类。
- 前端版本更新至 `bootstrap.js?v=156`；Java 测试、JS 语法检查、`git diff --check` 通过，本地服务 HTTP 200。
- 当前改动未提交、未推送、未部署生产。

## 2026-07-20 评分导航与排序调整（本地）

- 左侧导航“待评分”已改名为“评分”，评分业务页面和权限逻辑不变。
- 评分列表排序改为：计划完成时间升序 → 同日期待评分优先 → 子任务 ID 倒序。
- 前端资源版本更新至 `bootstrap.js?v=157`；Java 测试、JS 语法检查、`git diff --check` 通过，本地服务 HTTP 200。
- 当前改动未提交、未推送、未部署生产。

## 2026-07-20 工作台子任务总数显示修正（本地）

- 修正工作台第二排第一张卡片错误使用 `channelTasks` 导致显示 0 的问题。
- 卡片已恢复为“子任务总数”，使用统一统计字段 `stats.allTasks`；待处理和已完成卡片保持不变。
- 前端资源版本更新至 `bootstrap.js?v=158`；Java 测试、JS 语法检查、`git diff --check` 通过，本地服务 HTTP 200。
- 当前改动未提交、未推送、未部署生产。

## 2026-07-20 用户管理 pageresult 报错修复（本地）

- 已核对用户管理模块，源码使用的是正确的驼峰变量 `pageResult`；`pageresult is not defined` 来自浏览器缓存的旧前端资源。
- 已将 `admin-users.js` 资源版本更新至 `v159`，同时更新 bootstrap 入口版本，强制加载最新用户管理模块。
- Java 测试、JS 语法检查、`git diff --check` 通过；本地服务已重启并 HTTP 200。
- 当前改动未提交、未推送、未部署生产。

## 2026-07-20 管理员用户管理作用域修复（本地）

- 确认管理员进入用户管理页面报错的真实根因：`pageResult` 在 `try` 块内使用 `const` 声明，但分页模板在块外访问，触发 `pageResult is not defined`。
- 已将分页结果移到函数作用域并提供空结果兜底，接口失败时页面也能正常渲染空列表而不再抛引用错误。
- 资源版本更新至 `admin-users.js?v=160`；Java 测试、JS 语法检查、`git diff --check` 通过，本地服务 HTTP 200。
- 当前改动未提交、未推送、未部署生产。

## 2026-07-20 供应链徽章与部门负责人校验修复（本地）

- 供应链角色徽章前端增加统一角色归一化，兼容历史分隔写法并正确命中 `role-supplychain` 样式。
- 组织架构负责人候选继续按部门关联角色筛选，并在后端创建/更新部门时强制校验负责人角色必须与部门角色一致，管理员不能担任业务部门负责人。
- 前端资源版本更新至 `admin-users.js?v=161`、`admin-org.js?v=161`、`bootstrap.js?v=161`。
- 待执行 Java 测试、JS 语法检查、`git diff --check` 和本地服务重启验证；当前改动未提交、未推送、未部署生产。

## 2026-07-20 用户管理供应链徽章兼容修复与管理员负责人候选调整（本地）

- 发现用户管理接口返回中文角色值“供应链”时，原角色类名会变为空，导致文字显示为黑色且无徽章。
- 用户管理和组织架构角色类名现兼容中文“供应链”及英文角色值；CSS 版本更新为 `app.css?v=24`。
- 组织架构负责人下拉现展示部门匹配角色用户，并将管理员追加到候选列表末尾；后端允许管理员担任任意部门负责人。
- 待执行测试和本地服务重启验证；当前改动未提交、未推送、未部署生产。

## 2026-07-20 角色值归一化与负责人候选稳定性修复（本地）

- 用户管理徽章、角色显示和筛选改为统一角色键，兼容中文、大小写及分隔符形式，修复供应链只显示黑字的问题。
- 组织架构负责人候选同样按统一角色键匹配，并始终将管理员追加到候选列表末尾。
- 前端资源版本更新至 `bootstrap.js?v=162`；待执行完整测试和本地服务重启验证。

## 2026-07-20 角色显示与负责人分组修复（本地）

- 用户管理内置角色标签不再被后台角色展示名覆盖，产品推广固定显示中文；供应链继续使用明确的徽章角色键。
- 组织架构负责人下拉在管理员候选前增加不可选择的“管理员”分组标题，并保留管理员名字可选。
- 前端资源版本更新至 `bootstrap.js?v=163`；待执行完整测试和本地服务重启验证。

## 2026-07-20 管理员分组重复与供应链徽章样式修复（本地）

- 删除负责人下拉中重复的不可选“管理员”选项，仅保留不可选择的 `optgroup` 标题，管理员姓名仍可选择。
- 供应链徽章增加明确边框并提升背景色、字体色优先级，避免被表格通用样式覆盖；CSS 版本更新为 `app.css?v=25`。
- 前端资源版本更新至 `bootstrap.js?v=164`；待执行测试和本地服务重启验证。

## 2026-07-20 供应链真实角色值修复（本地）

- 查询测试库发现供应链用户存在历史角色值 `supply`，导致此前 `role-supplychain` 样式始终无法命中。
- 用户管理和组织架构现将 `supply`、`supplychain`、`supply_chain`、`supply-chain` 统一映射为供应链角色键。
- 前端资源版本更新至 `bootstrap.js?v=165`；待执行测试和本地服务重启验证。

## 2026-07-20 上传框剪贴板图片导入（本地）

- 在统一项目上传模块中加入剪贴板图片处理：鼠标移入上传框后，从飞书、微信或截图工具复制图片即可粘贴上传。
- 仅接受剪贴板真实图片 Blob，原有点击、拖拽和本地文件上传逻辑不变。
- 前端资源更新至 `project-uploads.js?v=148`；待执行测试和本地服务重启验证。

## 2026-07-20 生产发布前迁移顺序修复

- 生产发布预检发现 `2026-07-18-add-my-subtask-publisher.sql` 的历史数据回填排在 `publisher_name/publisher_role` 建列之前，已调整为先建三列再回填。
- 生产应用 JAR 尚未替换；数据库备份已生成并校验，迁移将在修复后重新执行。

## 2026-07-20 子任务视图与上传交互发布完成

- 提交 `dfd84d9c2bcf936ae468bfaecefab3bb00dcb55b` 已推送 Gitee 并部署生产，生产 `/root/emie/release-sha.txt` 已记录同一提交。
- 生产数据库备份 `/root/emie/db-backup-before-373ed7e-20260720_115935.sql.gz` 已通过 `gzip -t`；发布人字段迁移已执行并验证三列存在。
- 生产应用 JAR 上传前后 SHA-256 均为 `dded226676ef5658b6a40690b84d23dfd0d2060d40f40b0daf9b92202d7f9840`；回滚包为 `/root/emie/app.jar.bak.pre-dfd84d9-20260720_120153`。
- 生产 `emie-app`、`emie-preview-converter` 均运行，公开配置 HTTP 200，首页加载 `bootstrap.js?v=165`，剪贴板图片上传模块可读取，发布后日志无新增 ERROR。
- 当前后续只需人工验收登录态下的子任务、组织架构、角色徽章和飞书图片粘贴上传；本地与生产均已更新。

## 2026-07-20 产品企划子任务只读页面报错修复（本地）

- 根因：`renderDesignerTasks` 的 `readOnly` 参数未传入独立的 `renderDesignerTaskCards`，卡片模板直接引用未定义变量。
- 已将只读状态显式传入首次渲染和筛选刷新，修复产品企划打开全部/待处理子任务页面时报 `readonly is not defined`。
- 前端资源更新至 `dashboard-designer.js?v=166`、`bootstrap.js?v=166`；待执行本地测试，尚未提交、推送或部署生产。

## 2026-07-20 产品企划子任务只读页面修复已发布

- 修复提交 `2657128eb05ffa62b69259e96a87a867ab503a23` 已推送 Gitee 并部署生产。
- 生产 JAR SHA-256 为 `41cd0727a83c52b9e022a79dc88371487161adb28848d10378a7ca9a3a1168ae`，回滚包为 `/root/emie/app.jar.bak.pre-2657128-20260720_133934`。
- 生产首页已加载 `bootstrap.js?v=166`，公开配置 HTTP 200，两个容器正常运行，发布后日志无新增 ERROR。

## 2026-07-20 其他子任务 403 体验修复（本地）

- 根因：导航对普通成员展示“其他子任务”，但后端只允许管理员及销售/企划部门负责人读取部门任务，普通成员因此收到 403。
- 无部门负责人可见范围时接口现返回空列表，权限范围计算仍在后端执行，不扩大任何数据访问；部门负责人和管理员逻辑不变。
- 前端资源更新至 `dashboard-lists.js?v=167`、`bootstrap.js?v=167`；待执行本地测试，尚未提交、推送或部署生产。

## 2026-07-20 其他子任务 403 体验修复已发布

- 提交 `66090a483c44ded4f98d496e55d32eea444e465d` 已推送 Gitee 并部署生产。
- 生产 JAR SHA-256 为 `6a1f9dcd1c97afc6cfaee9a0e061b63d9f41330b9c9955f90473fd81f39ea8b0`，回滚包为 `/root/emie/app.jar.bak.pre-66090a4-20260720_135308`。
- 生产首页已加载 `bootstrap.js?v=167`，公开配置 HTTP 200，两个容器正常运行，发布后日志无新增 ERROR。

## 2026-07-20 普通成员隐藏其他子任务入口（本地）

- 普通销售和产品企划成员不再显示“其他子任务”导航；管理员及匹配关联角色的部门负责人继续显示。
- 前端使用已加载的部门数据判断负责人身份，后端接口权限保持不变；若当前已停留在无权限页面会自动回到工作台。
- 前端资源更新至 `core-shell.js?v=168`、`bootstrap.js?v=168`；待执行本地测试，尚未提交、推送或部署生产。

## 2026-07-20 普通成员隐藏其他子任务入口已发布

- 提交 `850874141c72894fa4acb0de3299f5c5f554f201` 已推送 Gitee 并部署生产。
- 生产 JAR SHA-256 为 `47b20327a4772468ed183a8cb6dd6dc6b3c847a2ef3d66a73d9aede9eddc5ed7`，回滚包为 `/root/emie/app.jar.bak.pre-8508741-20260720_140430`。
- 生产首页已加载 `bootstrap.js?v=168`，公开配置 HTTP 200，两个容器正常运行，发布后日志无新增 ERROR。

## 2026-07-20 图片渲染性能优化（本地）

- 生产诊断发现多张 3840x2304 PNG 单张约 7.6MB，详情页虽显示 60/80px 缩略尺寸却仍下载原图；下载接口也没有浏览器缓存头。
- 新增 `FileThumbnailService` 和受权限保护的 `/api/files/thumbnail/{fileName}`，首次生成 480px JPEG 缩略图并缓存 7 天。
- 项目/子任务详情默认使用缩略图，点击图片通过 `data-full-src` 加载原图；原上传、下载和原图预览逻辑保持不变。
- 前端资源更新至 `project-detail.js?v=169`、`bootstrap.js?v=169`；待执行完整测试，尚未提交、推送或部署生产。

## 2026-07-20 公司常规品二次评分通知（本地）

- 根因：产品企划评分后虽然会创建管理员的 `ScoringRecord` 并将子任务切换为 `planner_approved`，但 `activateSecondReview` 原先没有触发 `REVIEW_PENDING` 通知；通知服务也只有单个用户收件人入口，因此全部管理员不会收到提醒。
- 修复：二次评分首次激活时，常规品通过通知工作流查询全部有效管理员并逐人发送站内通知；开启飞书通知时逐人投递飞书卡片，并保留每位收件人的独立投递与审计记录。已增加状态判断，重复调用不会重复触发。
- 渠道定制仍通知销售负责人；本次未修改生产环境，待本地验证后按用户指示提交、推送或部署。

## 2026-07-20 公司常规品二次评分通知已发布

- 提交 `dec6113` 已部署生产，生产 `release-sha.txt` 已核验为 `dec6113`；旧生产提交为 `8508741`。
- 发布前数据库备份 `/root/emie/db-backup-before-dec6113-20260720_143023.sql.gz` 已生成并通过 `gzip -t`，旧应用回滚包已保留。
- `emie-app`、`emie-preview-converter` 均正常运行，公开配置 HTTP 200；生产应用确认使用 Java 21 启动，Tomcat 8080 正常监听，发布后启动日志无 ERROR。
- 本次未包含此前本地未提交的图片缩略图优化改动；该改动仍保留在本地工作区，未推送、未部署。

## 2026-07-20 生产备份与垃圾清理

- 生产清理前磁盘使用率约 60%，清理后约 54%，可用空间从约 15GB 增至约 18GB。
- 保留最近两份有效数据库备份：`db-backup-before-373ed7e-20260720_115935.sql.gz`、`db-backup-before-dec6113-20260720_143023.sql.gz`；保留最近两份应用回滚包：`app.jar.bak.pre-8508741-20260720_140430`、`app.jar.bak.pre-dec6113-20260720_143023`。
- 已删除更早的应用回滚包、无效数据库备份、历史 heap dump、旧 Java 构建产物，以及确认未被容器使用的旧 EMIE/MySQL 镜像。
- 未清理 Docker 全局构建缓存（仍约 7.3GB 可回收），因为该缓存可能被服务器其他项目复用；应用与预览容器清理后仍正常运行，公开配置 HTTP 200。

## 2026-07-20 待评分徽章统计修复（本地）

- 根因：工作台/导航徽章原先只按 `ScoringRecord.reviewStatus=pending` 计数，没有校验子任务当前是否仍处于该角色有效评分阶段；历史残留记录可能被计入，但评分页面按当前任务阶段展示，导致数量不一致。
- 修复：管理员仅统计常规品 `planner_approved`，销售仅统计渠道定制 `planner_approved`，产品企划仅统计 `delivered` 阶段，并保留未评分字段兼容判断。
- 当前改动仅在本地，未提交、未推送、未部署；生产仍为 `dec6113`。工作区此前图片缩略图优化未纳入本次改动。

## 2026-07-20 本地测试库项目编号迁移

- 已执行 `db/migrations/2026-07-20-add-project-code.sql` 到远程共享测试库 `designpmtest`，28 个项目全部补齐编号，范围为 `EMIE2026070001` 至 `EMIE2026070028`。
- 已重启本地 Java 21 服务并确认 `http://localhost:8080` 就绪；刷新页面后历史项目应显示新编号。
- 迁移前测试库备份因 `emietest` 无权读取 `column_masking_policy` 而未生成有效备份，不作为恢复点；本次仅修改项目编号字段和唯一索引，未操作生产库。

## 2026-07-20 项目编号搜索（本地）

- 项目列表服务端关键词查询和前端本地过滤均已加入 `projectCode`，全部项目、渠道定制单、公司常规品可直接搜索 `EMIEyyyyMM####`。
- 前端资源更新至 `dashboard-lists.js?v=171`、启动入口 `bootstrap.js?v=171`；已完成本地修改，待测试后再决定提交和发布。

## 2026-07-20 子任务归属筛选简化（本地）

- “待处理/全部子任务”保留全部任务、待认领、我的任务三个归属选项；“已完成子任务”隐藏该筛选，因为已完成任务不存在待认领状态。
- 前端资源更新至 `dashboard-designer.js?v=172`、启动入口 `bootstrap.js?v=172`；当前仅本地修改，未提交、未推送、未部署。

## 2026-07-20 子任务管理分页化（本地）

- 原“子任务管理”页面通过 `GET /api/projects` 一次性加载全部可见项目；现改用 `GET /api/projects/page`，默认每页 15 条，并保留状态、类型、关键词和日期筛选。
- 增加分页切换和查询中状态；旧接口保留给其他兼容页面，未直接删除。
- 前端资源更新至 `dashboard-lists.js?v=173`、启动入口 `bootstrap.js?v=173`；待执行测试和本地重启验证。

## 2026-07-20 项目编号与子任务分页已发布

- 提交 `31f4140` 已推送并部署生产，生产 `release-sha.txt` 已核验为 `31f4140`。
- 项目编号迁移已在生产执行并验证 34 个项目全部具备 `EMIEyyyyMM####` 编号；数据库备份和应用回滚包已保留。
- 子任务管理已改为服务端分页；编号搜索、待评分统计修复、筛选简化和图片缩略图优化随本次发布生效。
- 生产应用与预览容器正常，公开配置 HTTP 200，Java 21 启动正常，发布后启动日志无 ERROR。

## 2026-07-20 上传区域提示优化（本地）

- 创建项目、编辑项目和子任务中的图片/附件上传提示已统一为“拖拽到此处，或点击选择”，与实际支持的交互一致。
- 前端资源更新至 `project-form.js?v=174`、`project-tasks.js?v=174`、启动入口 `bootstrap.js?v=174`；当前仅本地修改，未提交、未推送、未部署。

## 2026-07-20 上传区域提示优化已发布

- 提交 `899d58b` 已推送并部署生产，生产 `release-sha.txt` 已核验为 `899d58b`。
- 生产加载 `bootstrap.js?v=174`，创建项目、编辑项目和子任务上传区域均使用新的拖拽/点击提示。
- 应用与预览容器正常，公开配置 HTTP 200，Java 21 启动正常，发布后启动日志无 ERROR。

## 2026-07-20 飞书同步游标修复已发布

- 提交 `2f21b58` 已推送并部署生产，生产 `release-sha.txt` 已核验为 `2f21b58`。
- 发布前数据库备份和应用回滚包已保留；应用与预览容器正常，公开配置 HTTP 200。
- 发布后日志未再出现 `Incorrect DATETIME`；同步队列历史失败数仍为 3 条，未发现新增同类失败。

## 2026-07-20 系统时区统一（本地）

- 根因：生产主机为北京时间，但应用容器默认 UTC，`LocalDateTime.now()` 产生的日志时间比北京时间少 8 小时。
- 已将本地启动脚本和生产 Docker Compose 的 JVM 固定为 `-Duser.timezone=Asia/Shanghai`，并设置容器 `TZ=Asia/Shanghai`。
- 当前改动仅在本地，待测试后提交、推送和部署生产。

## 2026-07-20 系统时区统一已发布

- 提交 `6ca3a45` 已推送并部署生产；生产 Compose 独立配置也已同步加入 `TZ=Asia/Shanghai` 和 `-Duser.timezone=Asia/Shanghai`。
- 生产主机与应用容器时间已核验一致，均为北京时间；Java 日志已显示 `+08:00`，不再相差 8 小时。
- 生产应用和预览容器正常，公开配置 HTTP 200；Compose 原配置备份已保留。

## 2026-07-20 飞书同步游标修复（本地）

- 生产检查发现 `feishu.sync.cursor` 缺失时使用 `LocalDateTime.MIN`，MySQL 报 `Incorrect DATETIME value: 169087565-03-15...`，导致定时增量对账失败。
- 已改为使用 `2000-01-01 00:00:00` 安全回溯时间；游标缺失或格式异常时记录告警，后续成功对账会持久化当前时间。
- 当前修改仅在本地，待测试后再提交、推送和部署生产。

## 2026-07-20 全系统冗余扫描

- 未发现可以直接删除的整块业务模块；31 个前端模块均有启动入口或被域门面/页面调用。
- 发现旧版 `GET /api/projects` 全量接口仍被“子任务管理”兼容页面使用，属于高优先级收敛候选，但不能直接删除，需先将该页面迁移到分页接口。
- `findAll()` 主要集中在用户缓存、管理员配置/角色、分享链接和项目旧接口；用户/配置数据量小，当前不是性能瓶颈，项目旧接口是主要优化对象。
- 工作台角色状态接口的两个调用点分别用于主面板和用户任务弹窗，不属于同一加载流程的重复请求；暂不删除。
- 本次为只读审计，未删除功能或文件；现有本地未提交改动继续保留。

## 2026-07-23 旧项目详情 500 兼容修复（本地）

- 根因：本地数据库中的历史项目在新增项目流程字段后，`workflow_stage` 和 `workflow_status` 仍可能为 `NULL`；流程服务使用不可包含空值的 `List.of(...).contains(null)` 判断，触发空指针并令项目详情返回 500。
- 修复：读取历史项目时为空或非法阶段提供安全回退；进行中的项目回退为“设计/进行中”，已完成项目回退为“大货/已完成”。项目后续更新时也会自动补齐默认流程值。
- 已增加历史空值项目的回归测试；当前仅本地修改，未提交、未推送、未部署生产。

## 2026-07-23 子任务所属阶段（本地）

- 新建子任务时新增必选“所属阶段”，可选设计、设计送审、打样送审、产品宣发、大货；编辑子任务时也可调整，后端同步做白名单校验，避免绕过前端写入非法阶段。
- 项目尚无子任务时，子任务进度的五个阶段全部显示“待进行”；添加首个子任务后，以其所选阶段开始推进。子任务卡片会明确展示所属阶段。
- 历史子任务在迁移时回填为“设计”；前端缓存更新至 `bootstrap.js?v=205`、`project-detail.js?v=175`、`project-tasks.js?v=178`。当前仅本地修改，未提交、未推送、未部署生产。

## 2026-07-23 待进行阶段视觉补全（本地）

- 子任务进度和项目总进度中的“待进行”节点不再使用无内容的实心灰点，统一改为浅色底、灰色描边并在圆内显示阶段序号；进行中、已完成和驳回样式保持原有语义。
- 前端缓存更新至 `bootstrap.js?v=206`、`project-detail.js?v=176`；当前仅本地修改，未提交、未推送、未部署生产。

## 2026-07-23 子任务阶段聚合与项目完结联动（本地）

- 子任务进度不再依赖手动推进的项目级游标，而是按每个阶段实际包含的子任务聚合：无任务为待进行；有未完成任务为进行中；该阶段最后一个子任务完成后才显示已完成；任一子任务被驳回时显示异常。
- 已完成阶段新增同阶段子任务后，会因为该阶段不再是全量完成而自动回到进行中；阶段节点同时展示“已完成数/总数”。
- 项目只有在“大货”阶段至少存在一个子任务、大货阶段全部完成且项目全部子任务完成时，才会从总进度的“子任务进行中”进入“项目完结”。历史上已经完结的项目保持完结状态。
- 前端缓存更新至 `bootstrap.js?v=207`、`project-detail.js?v=177`；当前仅本地修改，未提交、未推送、未部署生产。

## 2026-07-23 子任务驳回记录入口已撤回（本地）

- 按用户选择，仅移除刚增加的“查看驳回记录”按钮、弹窗及详情聚合数据；原有子任务驳回按钮、驳回流程、当前驳回意见和项目操作日志保持不变。
- 前端缓存更新至 `bootstrap.js?v=209`、`project-detail.js?v=179`；当前仅本地修改，未提交、未推送、未部署生产。

## 2026-07-23 新增 3D 送审阶段（本地）

- 在“设计送审”和“打样送审”之间新增“3D送审”，子任务进度由五阶段扩展为六阶段。
- 新建和编辑子任务的所属阶段选择、子任务卡片阶段名称、后端白名单及审核阶段定义已同步加入 `three_d_review`。
- 前端缓存更新至 `bootstrap.js?v=210`、`project-detail.js?v=180`、`project-tasks.js?v=179`；当前仅本地修改，未提交、未推送、未部署生产。

## 2026-07-23 子任务负责人增加产品推广（本地）

- 新建和编辑子任务的负责人类型新增“产品推广”，选择后仅展示产品推广角色人员；子任务卡片增加对应紫色角色标识，产品推广负责人可正常接单、交付和重新交付。
- 后端负责人类型白名单加入标准角色键 `promotion`，并兼容历史 `Promotion`、`product_promotion`、`product-promotion` 角色别名，避免人员与负责人类型校验误判。
- 前端缓存更新至 `bootstrap.js?v=211`、`project-form.js?v=180`、`project-detail.js?v=181`、`project-tasks.js?v=180`；当前仅本地修改，未提交、未推送、未部署生产。

## 2026-07-23 六阶段子任务进度已发布

- 功能提交 `7106c73a02fc51a720d0ccaf8ab1bb21ba3ac9e5` 已推送 Gitee 并部署生产；生产由 `961cd1b` 升级到该提交。
- 生产数据库备份 `/root/emie/db-backup-before-7106c73-20260723_174342.sql.gz` 已通过非空和 `gzip -t` 校验；项目流程迁移执行后字段、表和历史数据空值均验证正常。
- 生产应用 JAR 与本地产物 SHA-256 一致，旧 JAR 回滚包已保留；应用和预览容器 running、重启次数 0，首页、公开配置和预览健康接口 HTTP 200，前端资源版本为 `v211`。
- 本机已按用户要求从官方源码安装 `sshpass 1.10` 到 `~/.local/bin`。本地开发服务仍运行在 `8080`；发布未触发全员飞书通知。

## 2026-07-23 子任务逐次驳回快照（本地）

- 子任务卡片改为直接逐行展示每一次驳回，单行包含轮次、意见摘要、驳回时间和详情入口；点击后查看该轮提交内容、图片、附件、提交人、驳回人和完整意见。
- 每次驳回会把当时的交付内容写入该子任务对应的不可覆盖审计日志快照，重新交付不会覆盖旧记录；改造前的历史日志仍可显示，但缺少独立快照时只能用当前可恢复内容兜底并明确提示。
- 前端缓存更新至 `bootstrap.js?v=212`、`project-detail.js?v=182`。当前仅本地修改，未提交、未推送、未部署生产。

## 2026-07-23 三类项目列表整行打开详情（本地）

- 渠道定制单、公司常规品和设计/送审需求列表的整行均可点击打开详情，回车和空格键也可触发；查看、接单、暂停等行内按钮会阻止事件冒泡，不会重复打开详情。
- 设计/送审需求原先没有详情接口且“查看”会错误进入普通项目详情；现新增参与人/管理员可见的独立详情接口和弹窗，展示编号、状态、产品名称、负责人、客户、截止时间及需求描述。
- 前端缓存更新至 `bootstrap.js?v=213`、`dashboard-projects.js?v=158`、`dashboard-lists.js?v=180`。当前仅本地修改，未提交、未推送、未部署生产。

## 2026-07-23 驳回快照与整行详情交互已发布

- 功能提交 `a38d9ff72e8a89b007195a5dff2d4674fc503518` 已推送 Gitee 并部署生产，生产由 `7106c73` 升级到该提交。
- 生产数据库备份 `/root/emie/db-backup-before-a38d9ff-20260723_180350.sql.gz` 已通过校验；本次无表结构变更。旧 JAR 回滚包 `/root/emie/app.jar.bak.pre-a38d9ff-20260723_180417` 已保留。
- 生产应用与预览容器 running、重启次数 0；首页、公开配置和预览健康接口 HTTP 200，受保护接口未登录返回预期 401，生产前端入口为 `bootstrap.js?v=213`。
## 2026-08-06 系统管理导航单列适配（已发布）

- 按笔记本截图修复系统管理内部导航：所有分组和菜单强制单列排列，左侧导航面板独立纵向滚动，禁止横向溢出；“日志、分享管理、工作量”等项目继续归属左侧“运维与审计”分组。
- 系统管理导航列改为自适应宽度，长菜单名称在列内换行；入口缓存版本更新至 `app.css?v=40`。
- 本地构建和 121 项测试通过；提交 `1a038020e895685de25654065be71d726c64dc5d` 已推送并部署生产。生产旧提交为 `55be0cb...`，备份目录为 `/home/emie/emie-deploy-backups/20260806_143307`，公网检查 HTTP 200。
## 2026-08-06 项目群操作状态刷新（已发布）

- 修复创建/解散项目群成功后未传入项目 ID，导致项目详情按钮状态不及时更新的问题。
- 修复项目已终止或待终止时仍显示“创建项目群”按钮的问题。
- 前端缓存更新至 `project-detail.js?v=230`；提交 `052ef8884701b00935010d2013debd8af2e68069` 已推送并部署生产，生产公网检查 HTTP 200，备份目录为 `/home/emie/emie-deploy-backups/20260806_163732`。
## 2026-08-07 快速切换页面渲染竞态（已发布）

- 截图表现为左侧高亮“工作台”但右侧显示“渠道定制单”，根因是快速点击时旧页面请求晚返回并覆盖新页面 DOM。
- 已在主渲染、项目列表渲染和运行时状态中加入递增 `renderId` 校验；只有最后一次导航渲染允许写入页面。
- 已更新 `core-runtime.js?v=159`、`dashboard-projects.js?v=161`、`dashboard-lists.js?v=203`；全部前端 JavaScript 语法检查和 `git diff --check` 通过。
- 本地测试容器可正常启动，应用日志显示 Spring Boot 已在 8080 启动；提交 `b98e0a86785a5029f9dea93870055f241657818a` 已推送并部署生产，公网检查 HTTP 200，备份目录为 `/home/emie/emie-deploy-backups/20260807_102330`。

# 2026-08-07 飞书同步待处理队列修复（已发布）

- 根因：新同步任务的 `nextRetryAt` 默认为 `null`，原 Spring Data 查询仅匹配 `nextRetryAt <= now`；SQL 对 `NULL` 的比较不成立，导致新任务长期停留在 `pending`。
- 修复：主队列与后台队列共用的 `SyncQueueOperations` 查询改为同时领取 `nextRetryAt IS NULL` 的新任务和退避时间已到的重试任务；`SyncWorker` 与 `SyncQueueService` 已切换到新查询。
- 验证：Java 21 `clean package` 成功，121 项测试通过，`git diff --check` 通过；提交 `27818b0ed699175a96710d3b16419907b37fb900` 已推送并部署生产。
- 说明：Worker 当前在一个事务中依次写入 `processing` 和最终状态，因此管理概览可能观察不到短暂的处理中数量；这不再影响任务领取和实际同步。
- 生产：版本 `1.2.9`，数据库备份 `/home/emie/emie-deploy-backups/20260807_171158`；容器 running、restart=0、OOM=false，健康接口和公网 HTTP 200，启动后 Worker 已成功领取 20 条待处理任务。
