# EMIE 工作接续记录

> 用途：跨设备、跨网络环境继续开发时，先阅读本文档，再查看 `git status` 和最近提交。

## 当前状态

- 当前分支：`project_manager_system`
- 当前生产版本：`1.6.2`
- 当前已发布提交：`fb033a6`
- 生产发布状态：已完成（1.4.1 缺陷修复批次含 V38/V39/V40；1.4.2 CSP 修复；1.4.3 积分上线前修复含 V41）
- 积分功能：已于 2026-08-16 正式启用（mode=ACTIVE，生效日 2026-08-17，冒烟验证通过）
- 测试环境状态：已通过 `./scripts/test-update.sh`
- 最近完整测试：222 个测试全部通过

## 最近完成

- 修复附件下载提示会话过期（2026-08-18）：下载/预览通过新窗口或原生链接打开时不会携带前端请求头中的 token，导致后端返回“未登录或会话已过期”；现为同源文件 URL 自动附加 `access_token`，复用后端现有认证校验。已通过 `./scripts/test-update.sh`。

- 修复管理员看板展示停用用户（2026-08-18）：管理员状态看板原先直接读取角色全部用户，未过滤停用状态；现与其他角色统一仅展示 `active` 用户，停用用户不再出现在未分配部门或角色面板中。已通过 `./scripts/test-update.sh`。

- 优化管理员角色面板排版（2026-08-18）：将管理员工作台改为双列独立流式布局，左列展示销售、产品推广、设计师，右列展示产品企划、供应链，避免销售面板高度较大时在右侧产生大块空白。已通过 `./scripts/test-update.sh`。

- 补齐管理员产品推广面板（2026-08-18）：管理员工作台角色面板拼接遗漏 `promotion`，现补充产品推广状态看板；后端数据和其他角色面板逻辑保持不变。已通过 `./scripts/test-update.sh`。

- 隐藏完全空的执行区域（2026-08-18）：供应链等角色在普通子任务和设计需求都为空时，自动隐藏整个“我的子任务”区域，避免只剩一大片空白；任一类型有内容时仍正常展示。已通过 `./scripts/test-update.sh`。

- 优化设计师空任务提示（2026-08-18）：设计师没有可归入“我的子任务”的普通子任务、但存在设计/送审需求时，隐藏误导性的“当前没有需要处理的关联子任务”空状态，只展示实际存在的设计需求；有普通子任务时仍正常显示子任务面板。已通过 `./scripts/test-update.sh`。

- 修复不同任务表格列宽串扰（2026-08-18）：将五列子任务面板的固定列宽规则限定到专用表格类，避免误套到供应链等七列表格，恢复状态、驳回意见和操作列的正常显示。已通过 `./scripts/test-update.sh`。

- 微调设计需求列对齐（2026-08-18）：在保留“需求负责人”列的基础上重新分配六列比例，使负责人、设计师、产品企划、要求完成时间和状态列间距更均衡，并与工作台右侧基准线一致。已通过 `./scripts/test-update.sh`。

- 增加需求负责人列（2026-08-18）：设计/送审需求表新增“需求负责人”，读取设计需求的负责人字段；同时重新均衡六列布局，保留设计师、产品企划、要求完成时间和状态列对齐。已通过 `./scripts/test-update.sh`。

- 对齐设计/送审需求表（2026-08-18）：将该表按子任务面板的统一五列比例排版，需求、设计师、产品企划、要求完成时间和状态列保持稳定间距，状态列与其他工作台区域右侧对齐。已通过 `./scripts/test-update.sh`。

- 对齐子任务状态面板列（2026-08-18）：统一“待接单、进行中、待送审”等工作台子任务表格的五列布局，固定列宽比例和状态列宽度，使不同面板的表头与内容保持垂直对齐。已通过 `./scripts/test-update.sh`。

- 统一工作台时间列名称（2026-08-18）：将设计/送审需求、子任务和项目列表中的“要求完成”“要求时间”统一调整为“要求完成时间”，仅更新展示文案，不改变数据逻辑。已通过 `./scripts/test-update.sh`。

- 简化项目列表操作（2026-08-18）：移除“渠道定制单”和“公司常规品”表格的“查看”按钮，保留整行点击进入项目详情；删除操作列后重新分配其余列宽，状态列统一右侧对齐。已通过 `./scripts/test-update.sh`。

- 简化设计/送审需求操作（2026-08-18）：移除表格右侧“查看并处理”按钮，保留整行点击进入详情并在详情弹窗内执行操作，避免同一入口重复。项目表的“查看”按钮保持不变。已通过 `./scripts/test-update.sh`。

- 对齐工作台表格列（2026-08-18）：统一“设计/送审需求”、渠道定制单和公司常规品表格的状态、操作末两列宽度，避免不同字段数量导致右侧列错位；其余列继续自适应分配空间。已通过 `./scripts/test-update.sh`。

- 优化工作台空白区域（2026-08-18）：无可展示成员的角色状态面板自动隐藏；状态面板统一按实际内容高度排列，不再被同一行其他角色强行撑高；保留角色间距和响应式布局。已通过 `./scripts/test-update.sh`。

- 同步生产版本号（2026-08-18）：通过生产发布脚本预检读取生产当前版本为 `1.6.2`；本地测试容器 `APP_VERSION` 已统一为 `1.6.2`，避免测试环境继续显示旧版本。未执行生产发布或重启。

- 补充送审后验收入口（2026-08-18）：产品企划打开“送审中”子任务详情时新增“通过并评分”按钮，复用现有 `taskApprove` 评分/验收流程；设计师不会显示该按钮。已通过 `./scripts/test-update.sh`。

- 修复送审后主页任务状态同步（2026-08-18）：送审成功后除更新详情弹窗外，额外局部重载企划“我的子任务”区域，使任务从“待送审”立即移动到“送审中”；不触发整个工作台刷新。已通过 `./scripts/test-update.sh`。

- 修复企划送审后的详情刷新（2026-08-18）：送审成功后改为只重新获取当前子任务并重绘详情弹窗，状态更新为“送审中”且按钮消失；不再触发整个工作台刷新，保留用户当前页面位置和上下文。已通过 `./scripts/test-update.sh`。

- 修正产品企划送审入口（2026-08-18）：确认设计师只负责交付，产品企划负责将 `delivered` 状态的子任务正式送审；详情弹窗接入已有 `/submit-review` 流程，企划视角显示“送审”按钮，后端继续校验项目负责人企划权限。已通过 `./scripts/test-update.sh`。

- 修复待送审详情缺少送审按钮（2026-08-18）：详情弹窗原先只允许 `designer` 且未校验负责人，导致供应链/产品推广或部分设计师视角缺少操作入口；现统一支持设计师、供应链、产品推广在本人负责且任务为 `accepted` 时显示“送审”按钮。已通过 `./scripts/test-update.sh`。

- 调整工作台顺序（2026-08-18）：根据使用优先级将 P2/P3“我的子任务”区域移到 P1“我的项目”区域上方，设计师/企划进入工作台先处理具体待办，再查看项目整体信息。已通过 `./scripts/test-update.sh`。

- 工作台项目/子任务分区（2026-08-18）：将 P1 项目类内容统一放入“我的项目”区域，将 P2/P3 执行内容统一放入“我的子任务”区域；新增分区标题、层级说明和不同浅色背景，保留原有数据、筛选与操作。相关文件：`dashboard-home.js`、`app.css`。已通过 `./scripts/test-update.sh`。

- 调整工作台内容顺序（2026-08-18）：将“设计/送审需求”从状态看板和任务列表之间移动到页面最底部，优先展示任务处理内容，需求表作为补充信息。相关文件：`dashboard-home.js`。已通过 `./scripts/test-update.sh`。

- 设计师任务面板首版整理（2026-08-18）：新增任务状态摘要卡；任务按待接单、进行中、待送审、送审中、已完成分组折叠，前四组默认展开、已完成默认收起；保留原有筛选、任务详情和操作。相关文件：`dashboard-designer.js`、`app.css`。已通过 `./scripts/test-update.sh`。

- 修复产品推广角色归一化与状态看板（2026-08-18）：补齐企划查看产品推广团队的权限范围；移除 `/api/users` 对历史 `Promotion` 键的重复输出，避免 flatten 后重复人名；统一 `Promotion`/`product_promotion`/`product-promotion` 为 `promotion`；补齐旧状态面板的“产品推广”中文映射，并固定角色字典使用标准中文名称。销售、企划、设计师、供应链路径同步检查，未发现相同的角色分组遗漏。已通过 `./scripts/test-update.sh`。

- 企划视角补充产品推广状态看板（2026-08-18）：状态汇总接口加入 `promotion` 角色；产品推广人员按进行中子任务显示忙碌/空闲状态；企划工作台新增“产品推广状态看板”，并沿用自适应双列布局。相关文件：`DashboardController.java`、`ProjectService.java`、`ProjectController.java`、`dashboard-home.js`。已通过 `./scripts/test-update.sh`。

- 工作台状态面板自适应布局（2026-08-17）：优化产品企划、供应链、设计状态看板的排版。状态卡片按人员数量和内容高度自然伸缩；产品企划与供应链在左列独立纵向排列，设计状态在右列独立计算高度，避免少量人员面板被大量空白撑高；窄屏自动切换为单列。相关文件：`src/main/resources/static/js/dashboard-home.js`、`src/main/resources/static/css/app.css`。已通过 `./scripts/test-update.sh`（容器重建、健康检查、入口冒烟、弹窗与响应式回归）。

- 管理员手动调账（2026-08-16）：积分纠错新入口（无需用户提异议）。
  - 后端 POST /api/point-governance/manual-adjustment：admin 角色校验；userId 必须存在；points 非零整数且 |points|≤100000；reason 必填 ≤500 字；写 point_adjustment_ledgers（sourceType=MANUAL，sourceId 取 MAX+1，(source_type,source_id) 唯一索引兜底并发冲突转 409；createdBy 审计；accountingMonth 缺省入账当月）。
  - 前端管理端「积分与绩效配置」新增手动调账卡片+弹窗（成员下拉仅列启用成员、正负积分提示、备注必填 500 字内、前端校验、后端 error 文案透出）；用户积分页「调账与 PO 积分」MANUAL 来源显示为「管理员调账」。
  - 测试：新增 PointManualAdjustmentServiceTest（9 用例），全量 222 个测试全绿。
- 积分上线前审计与修复（2026-08-16）：P0 无（幂等双保险成立）；修复 6 个 P1 + 关键 P2。
  - 跨月归月回溯改写移除（BASE 入账时锁定归属月）；V41 调账表加 accounting_month 统一两表口径；PO 履职积分按进展月落账。
  - 供分不足标记管理入口（GET/PUT /api/performance/monthly + 前端卡片恢复）；绩效工资展示隐藏（管理员线下计算）。
  - 并发 APPROVE 唯一冲突显式 flush 转 409；effective-at 解析失败补 WARN 日志。
  - 前端积分规则必选（ACTIVE 前置）；归档目标统一为「设计师本月目标」（monthly_user_point_targets，回退 standard config）。
  - 生效日配置 points.effective-at=2026-08-17（application.yml）；生产清理 7 月任务 3 条流水（已备份服务器 /tmp）。
  - 生产数据验证：幂等 0 重复、23 规则启用、模式 TRIAL（8/1~9/30，ACTIVE 10/1 由产品决定提前）。
- 缺陷审计与修复（2026-08-16）：四领域并行审计（安全/并发事务/业务逻辑/前端）+ 4 名工程师并行修复 + 3 路复审 + P3 加固批次 + 测试缺口补齐。
- P0：积分异议重复入账（APPROVED 终审拦截 + V38 处理中唯一索引 + V40 APPROVED 唯一索引 + 并发兜底转中文业务异常）。
- P1×12：分享页反射 XSS（esc 转义 + CSP + share.css/share.js 外置）；前端存储型 XSS ×5（评分中心/管理后台/新建弹窗/操作日志/登录页品牌）；admin 子目录下载授权旁路；SyncWorker 事务边界；deleteSubTask 无锁删除；评分中心终验不发质量加分；月度归档 shortage 保护失效；异常分类缺失。
- 关键 P2×19：project_code 唯一约束（V39）+ 顺延重试；状态流转/流程审核/轮次计数行锁；退单行锁；通知 CAS 认领；归档临时文件 + ATOMIC_MOVE + 互斥；需求评分锁内重算；deleteProject 孤儿清理；质量阈值加权平均统一；前端转义补齐（escHtml(escJsString) 组合、renderId 竞态）。
- P3 加固：全站 CSP（script-src-attr 'none'，/share 页除外）；匿名 admin logo 闭环（AuthFilter 精确前缀 + 文件名正则白名单）；分享密码复杂度（≥6 位非纯数字）+ 前端校验与清除语义对齐；文件名校验收紧（拒绝引号/尖括号）；慢日志去 query；sanitizeText 事件属性白名单；输入清洗补漏（IP 选项/部门/PO 项目）；登出清理定时器/SSE；日期本地化（renderDatePicker/分享过期时间）；design_pm_user 无效缓存移除；内联事件迁移 CSS。
- 测试补齐：deleteSubTask 锁守卫、SyncWorker 逐条事务、通知 CAS、需求评分锁内重算、归档幂等/互斥/isRegularFile 守卫（新增 3 测试类 + 扩展 2 类，共 +31 用例）。
- 新迁移：V38（积分异议处理中唯一）、V39（项目编号唯一）、V40（积分异议 APPROVED 唯一）。
- 产品确认：REJECTED 复议保留；历史 QUALITY 不补发；加权阈值接受；V40 历史展示变化接受。

## 开始工作前

```bash
git status -sb
git pull --ff-only
sed -n '1,240p' WORKLOG.md
```

如果工作区存在未提交改动，先确认来源，不要直接覆盖或重置。

## 修改完成后

1. 更新本文档的“当前状态”“最近完成”或“下一步”。
2. 按项目要求运行相关测试。
3. 代码或静态资源修改后运行：

```bash
./scripts/test-update.sh
```

4. 记录测试结果、部署状态和遗留风险。
5. 未经明确授权，不自动提交、推送或发布生产。

## 下一步候选

- 补充分享链接有效期的服务层测试，覆盖缺失、超过 60 天和自定义时间。
- 评估将前端 API 的 `localStorage` token 逐步迁移为纯 Cookie 认证。
- 持续扫描业务流程错误页和未使用接口。
- 【产品确认】积分异议 REJECTED 复议规则是否保留；V40 对历史多笔 APPROVED 改为 REJECTED 的展示变化是否可接受。
- 【产品确认】质量阈值改加权平均后，角色权重不均衡任务的质量加分判定变化；历史经评分中心完成的任务缺 QUALITY 流水是否补发。
- 【后续批次 P3】匿名 logo 功能闭环（AuthFilter 放行或独立公开资产端点）；分享页个别插值（statusLabel/viewCount 等）转义补齐；regular 类型幽灵 plannerId 校验；主应用 CSP（script-src-attr 'none'）；服务端文件名校验限制引号/尖括号；event-runtime 黑名单改进；登出清理定时器/SSE；日期 UTC 偏差；慢日志 query 脱敏；IpOptionController/PoPointsService/DepartmentController 输入清洗；测试缺口补齐（deleteSubTask 锁、SyncWorker 条目事务、通知 CAS、需求评分锁、归档幂等）。

## 重要环境约定

- 生产发布使用：`./scripts/release-production.sh`
- 生产配置文件不纳入 Git：`.server.production.local.env`
- 不执行 `docker compose down -v`，避免删除持久化数据。
- 通过 VPN 访问项目时，优先使用 Git 同步；不要同时让两台设备修改同一份未提交文件。
